# Copy to .env.local . For local dev point at your local backend:
VITE_API_URL=http://localhost:8080
# After you deploy the backend, set this to the deployed URL (no trailing slash):
# VITE_API_URL=https://your-backend-url
