# Tasks — Amazon Second Life (48h: June 13–15)

MVP-only. One MAJOR TASK = one Fable session. Owners: **A** = backend (me + Fable) · **B** = frontend/demo · **C** = research/PRD/video. The ⭐ spine (MT1–MT3) is the must-have-by-hour-24 demoable core: **scan → delta-grade → VRS math → Health Card → radar ping**. If anything slips, cut from MT4 edges, never from the spine.

## HUMAN TASKS (not Fable — do these first, tonight June 13)

- [ ] **Photograph the demo item set** (owner: A+B, ~90 min, BLOCKS MT1 cache quality):
  8 real items, each with: 1 catalog-style shot (clean background), 2 "day-0" shots (good light, multiple angles), 2–3 "current" shots (show real wear where it exists).
  1. Hero: pair of worn-ish shoes (Priya, ₹500-class)
  2. Hero: baby monitor or any outgrown kid-gear/electronics you own (Rahul)
  3. Apparel item, ideally one whose color photographs differently than catalog-style lighting (kurta — listing diagnostics)
  4. A sealed Amazon box, tape intact (RTO lane)
  5.–8. Filler: book, headphones, bottle, backpack — anything (batch view; cached grades only)
  Drop into `backend/app/seed/images/SL-00X/` as `catalog.jpg`, `day0_1.jpg`, `day0_2.jpg`, `current_1.jpg`… Fable handles the rest. **Until photos land, MT1 builds against 2 stock-photo placeholder items — pipeline first, photos swap in.**
- [ ] Check email for AWS/Kiro credit codes; set AWS billing alert (A)
- [ ] OBS/Loom recording test (C)
- [ ] C starts citation verification for the `[VERIFY-C]` stats in docs/PRD.md

## MT1 ⭐ — Seed store + Product Passport + Delta-Grader (A) ✅ DONE (commit be97d15)
**Goal:** `POST /grade` returns real Nova-2 delta-grading JSON for seeded items, with cached fallback.
Scope: `seed/` module (items/orders/neighbors JSON + images), passport event store (in-memory + DynamoDB behind env flag), multimodal extension of `llm.py`, grading prompt + Pydantic schema + retry-on-invalid-JSON, cache capture script, `GET /items`, `GET /items/{id}`.
**Verify (done when):** local uvicorn: `POST /grade` for the shoe returns schema-valid JSON with ≥1 localized defect, confidence, same-unit block, `source: live-bedrock`; rerun with AWS creds removed → identical shape, `source: cached`; `GET /items` lists all seeded items. Grading consistency: same item graded 3× → same letter grade ≥2/3 (else tighten prompt before proceeding — this is the highest-risk item in the build, test in the first 6 hours).
**Result:** all checks PASS, fresh verifier signed off. Hero shoe D/D/D (3/3 consistency). 2 placeholder items (SL-001 shoe, SL-002 monitor) with Commons images + cached grades; SL-003..008 metadata only (no images → `/grade` 502 until photos land). ⚠️ Discovered: deploy is IAM-blocked (see top of file + STATE.md).

## MT2 ⭐ — VRS engine + Health Card + RTO + Radar + Pricing + deploy (A) ✅ DONE (this session)
**Goal:** every endpoint in docs/api-spec.md live on the Function URL.
Scope: `vrs.py` (6 paths, breakdowns, eligibility), `pricing.py` (depreciation, decay, liquidity curve), `healthcard.py` (warranty months from seeded invoice), `radar.py`, `inspection.py` (`/seal-check` + `/diagnose-listing`), `metrics.py`, endpoints `/route` `/health-card` `/seal-check` `/radar/{asin}` `/price-curve` `/diagnose-listing` `/metrics`. Redeploy via `./deploy.ps1`.
**Verify (done when):** all api-spec curls pass against the **deployed** Function URL; `/route` on the shoe shows `local_p2p` winner with the hero math; `/seal-check` on SL-004 returns `SEALED_NEW`; cached fallback verified post-deploy.
**Result:** all 9 verify checks PASS on the deployed Function URL, fresh verifier signed off. **Deploy blocker (IAM) RESOLVED** — `deploy.ps1` now pushes ECR + updates Lambda. VRS constants LOCKED (architecture.md §4): every path's `recovery` = exact sum of its breakdown. Hero shoe → `local_p2p` winner at every grade (grade D: local +₹83 vs warehouse −₹129; grade B target ≈ +₹279 vs +₹66). Sealed RTO mixer → `rto_relist` +₹2,464 (skips grading). Two AI endpoints serve hand-authored cached responses (`SL-004.seal.json`, `SL-003.diagnose.json`) until real photos land — the stage-safe path. ⚠️ Note: `/route` & `/health-card` read grade/route from the in-memory passport, which is per-Lambda-instance — sequential demo calls hit the same warm instance, so the spine flow works; a cold start resets to no-grade (call `/grade` first).

## MT3 ⭐ — Frontend spine: the demo console (B builds with Fable, A supports API) ✅ DONE (commit 722a605)
**Goal:** the 5-screen hero flow clickable end-to-end on Vercel, phone-frame, Amazon-look.
Screens: Returns inbox → item + guided-capture intro → **delta-grade screen (side-by-side day-0 vs now, defect callouts, confidence, same-unit badge)** → **VRS screen (6 paths animate in with rupee math, winner highlighted, "₹410 warehouse route defeated by ₹40 local hop")** → **Health Card (provenance, grading report, warranty-transfer badge)** → radar ping toast ("3 buyers within 4 km"). Brand: Amazon-adjacent palette, "Second Life" badge styling; design skills (frontend-design / ui-ux-pro-max) apply.
**Verify:** live Vercel URL: full click-through with real backend; then with `force_cached` → visually identical; no dead buttons on the spine; playwright/chrome-devtools pass over the flow.
**Result:** ALL verify checks PASS on the LIVE Vercel URL (https://amazon-hackon.vercel.app) + deployed Function URL, driven via playwright + chrome-devtools (localhost AND production). Full spine works: inbox (8 live items + /metrics banner) → guided-capture intro (live provenance) → delta-grade (live Bedrock D/95% with 3 defects AND instant cached D/90% — visually identical) → VRS (winner local_p2p +₹83, breakdown reconciles to the rupee, warehouse −₹129, "₹212 swing", 6 ranked paths with gated ineligibles, CO₂/km impact) → Health Card (warranty TRANSFERS badge, provenance, price-decay sparkline) → Idle Asset Radar ping toast. Every number from a live endpoint. **0 console errors** (6 benign image-404s → category-tile fallback). Built with React 19 + Tailwind v4, **zero new deps**, CSS-only motion; design system lives in `frontend/src/components/` for MT4 reuse. force_cached toggled via the LIVE/CACHED switch in the inbox header. DynamoDB write-through CONFIRMED (db-setup.md §4 ✅). ⚠️ Inbox wires ONLY the SL-001 hero path; other rows are display-only "QUEUED" (MT4 wires SL-002 radar + SL-004 RTO).

## MT4 — Frontend moments + supporting screens (B + Fable) ✅ DONE (commit 4f71166)
**Goal:** every remaining demo beat reachable.
Scope (cut from the bottom if time pressure): Rahul one-tap resell from order history + Idle Asset Radar ping screen + liquidity slider · keep-it decision screen ("keep them — buyer 4 km away, earn ₹250") · RTO Sealed Lane screen · listing-diagnostics comparison screen (auto-patch before/after) · mock PDP: "Second Life options near you" row + buyback badge + static size widget (30 min, no more) · "Your Things" dashboard ("your home holds ₹47,300 in dormant value") · green counter (minimal ticker) · locker/escrow state screen · batch metrics view ("8 items → ₹4,830 recovered vs ₹0 baseline").
**Verify:** docs/demo-and-prfaq.md script walkable end-to-end on Vercel with zero dead ends; every number on screen comes from the API, not hardcoded JSX.
**Result:** all verify checks PASS, walked end-to-end via chrome-devtools on localhost AND the LIVE Vercel URL. Built four tappable inbox lanes (`App.jsx` branches by item_id) + 5 new screens, all bound to live endpoints: **RadarScreen** (SL-002 `/radar` — demand + 12 dormant units + ₹20,600 + SVG radar) → **LiquidityScreen** (`/price-curve` — reactive slider, recommends ₹1,750) → ping; **SealLane** (SL-004 `/seal-check` SEALED_NEW 96%) → **RouteScreen** reused (rto_relist **+₹2,464**) → ping; **DiagnoseScreen** (SL-003 `/diagnose-listing` — 18 returns, navy→royal, −40% patch); **MetricsScreen** (`/metrics` — recovered vs write-off, bypass ring, green ledger). `api.js`: `routeRto` + `priceCurveSafe` (both re-run the right prereq on a cold-start 409). Spine (SL-001) regression-clean; 0 unexpected console errors (6 documented img-404s + 1 handled 409). Zero new deps; reused the MT3 design system. **CUT (per cut-from-bottom):** "Your Things" dashboard, standalone keep-it screen, mock PDP — no API backs their numbers and they're not in the 3-min beat sheet; escrow/locker beat folded into the ping-toast copy. ⚠️ `/metrics` is cumulative over the warm Lambda instance (drifts up across demo runs) — note for MT5.

## Pre-MT5 — Grading correctness hardening (A+B) ✅ DONE (June 14)
**Why:** user flagged the grader before MT5 (identical catalog photo → B; random image still graded). **Diagnosed** (root cause = demo data + defect-biased prompt + missing identity gate, NOT the pipeline) and **fixed + deployed + verified live.** See docs/lessons.md "Grading correctness hardening" and STATE.md "Done (this session — grading hardening)".
- **Model:** Gemini 2.5 Flash primary, Nova 2 Lite failover (Gemini **Pro** free-tier is 429-quota-walled → unusable as a live primary; Flash verified). Env flipped on Lambda + `.env`/`.env.example`; `GEMINI_MODEL=gemini-2.5-flash`.
- **Prompt:** delta-honest rubric + ignore lighting/angle/distance/background/shadows/image-quality (`grading.py`).
- **Gate:** `needs_human_review` + `review_reason` on not-same-unit / low confidence (non-blocking; `Grade.jsx` renders the reason).
- **Reliability:** Gemini JSON-mode + `thinking_budget=0` (thinking tokens were truncating JSON) + 18s timeout (`llm.py`).
- Re-captured SL-001/SL-002 grade caches; api-spec.md + architecture.md updated.
**Verified:** live `/grade` = `live-gemini / gemini-2.5-flash` with gate fields; cached mirrors live; spine `/route` SL-001 `local_p2p +₹83` unchanged; 3/3 consistency (match A/A/A, mismatch D/D/D).
**HUMAN TASK — ✅ DONE (June 14, audit follow-up):** real **same-shoe** photos for SL-001 landed in `backend/app/seed/images/SL-001/` + `frontend/public/items/SL-001/`; re-ran `python scripts/capture_cache.py SL-001`. SL-001 now grades `same_unit.verified:true`, **D** (real heavy wear, 4 defects, honest justification), `needs_human_review:false`; local_p2p **+₹83** vs warehouse **−₹129** unchanged. ⚠️ Needs a **backend redeploy + frontend push** to propagate the new images/cache to the live Lambda/Vercel (still serving the OLD Nike set until then).

## MT5 — Bulletproof + polish + repo (A+B)
**Goal:** the demo cannot fail on stage.
Scope: cached responses verified for all items × all AI endpoints; kill-switch env (`FORCE_CACHED=1`); failover drill (break the primary on a test invoke → failover → cache); loading/error states; mobile-frame polish pass; README with architecture diagram + live URLs; repo public; final deploy both ends.
**NOTE (post grading-hardening):** the provider chain is now **Gemini 2.5 Flash → Nova 2 Lite → cache** (not Bedrock-primary). The failover drill should break **Gemini** (e.g. bad `GEMINI_API_KEY` on a test invoke) → confirm Nova serves → then break both → confirm cache. The same-unit trust gate (`needs_human_review`/`review_reason`) is part of the loading/error-state audit.
**Verify:** full demo run on live URLs with the live model disabled → no visible difference; fresh-context verifier subagent walks the script and signs off; Lighthouse sanity on the Vercel app.

## MT6 — PRD final + demo video + submission (C owns, A+B support)
**Goal:** submitted.
Scope: `[VERIFY-C]` citations resolved in PRD; PRD mapped onto the official submission template section-by-section; 3-min video recorded per docs/demo-and-prfaq.md (+ backup recording); architecture diagram exported; team intro with personality; submit; dry-run pitch.
**Verify:** video ≤3:00, opens with Priya hook, shows live grade + VRS math + Health Card + radar ping; every template section answered; submission confirmed before deadline with ≥2h buffer.

## MT7 — Two-sided console: Prevent + Recirculate (Buyer hub + Seller dashboard) (A backend + B/Fable frontend)
**Why this exists:** today the demo tells ONE story — *return → recover value* (the live spine). MT7 widens it to the full lifecycle a product can hit, which is a stronger pitch and directly answers the PS ("Products Without a Second Chance"): **Prevent → Recover → Recirculate.** Recover is the existing spine; MT7 adds Prevent (seller return-rate dashboard + buyer size social proof) and Recirculate (buyer order-history → one-tap resell, the Rahul beat with a proper origin). Decided in the June 13 brainstorm session.

**THE IRON RULE STILL HOLDS — mock the DATA, not the ARCHITECTURE.** Every new number on screen comes from a real endpoint, NOT hardcoded JSX. For features with no real computation (size proof, seller return counts) the honest cheap path is a **seed JSON file + a one-line FastAPI route that returns it** (~15 min each). This keeps the frontend identical to the spine, survives a judge opening the network tab, and keeps MT5's bulletproof audit clean. Hardcoded JSX is the explicit fallback ONLY where genuinely nothing varies. Do NOT special-case "fake" components.

**Goal:** a persona switch on the phone frame reaches three views — **Returns-ops** (existing inbox/spine, unchanged), **Buyer hub**, **Seller dashboard** — all clickable end-to-end on Vercel against the deployed backend, every figure API-backed.

### Scope — Buyer hub (persona = Rahul)
1. **Browse / "add product" (buy view)** — a small storefront from seed items → **PDP** screen with:
   - **Size social proof block** ("68% of size UK-9 buyers ordered one size up") — applies to footwear/apparel (SL-001 shoe, SL-003 kurta). NEW endpoint `GET /size-advice/{asin}` backed by NEW `seed/size_signals.json` (per-asin size distribution + "% went one up/down" + recommendation copy). This is the buyer-side PREVENT moment (fewer fit returns).
   - Second Life buyback/resale hint ("resells for ~₹X near you later") — reuse `pricing.resale_value` or fold into size-advice payload; keep it API-backed.
   - "Add to cart / Buy" — visual only is fine (no order persistence required for the demo); if you want it to land in order history, append to an in-memory list.
2. **Order history** — Rahul's real orders already live in `seed/orders.json → rahul_order_history` (monitor `B0MONITOR1`/SL-002, stroller `B0STRLLR03`, crib `B0CRIB0042`). Expose via NEW `GET /orders/{persona}` (currently only `radar.py` reads orders.json internally — just surface it). Each order row → **Return** | **Resell**.
   - **One-tap Resell** on the monitor → flows straight into the EXISTING `RadarScreen` (`/radar/B0MONITOR1`) → `LiquidityScreen` (`/price-curve/SL-002`) → ping toast. This is the Rahul beat — MT4 built the destination, MT7 builds the natural entry point.

### Scope — Seller dashboard
1. **Catalog return-rate view** — the seller's SKUs sorted worst-first, each with units sold / returns / return-rate% / top return reason. NEW `seed/seller_catalog.json` + NEW `GET /seller/returns`. Hero high-return SKU = the kurta (SL-003, "colour not as shown" — DiagnoseScreen already cites 18 returns); shoe (SL-001, "size too small") is a second.
2. **Tap a high-return SKU → "Why this gets returned"** — REUSE the existing `/diagnose-listing` (already returns return reasons + navy→royal swatch fix + the −40% projected drop for SL-003). Show the reasons breakdown + AI-recommended fix + projected return-rate drop. This is the seller-side PREVENT moment.

### Backend work (thin, seed-backed — redeploy required, unlike MT4)
- NEW `seed/size_signals.json` + `size.py` (or fold into `seed.py`) → `GET /size-advice/{asin}`
- NEW `seed/seller_catalog.json` + `seller.py` → `GET /seller/returns` (drill-down reuses `/diagnose-listing`)
- NEW `GET /orders/{persona}` exposing `orders.json` rahul_order_history (reuse `radar.py`'s loader)
- Register new routers in `app/main.py`; **redeploy via `backend/deploy.ps1`** (Docker running) — MT7 is NOT frontend-only. Document the 3 new endpoints in `docs/api-spec.md` and `docs/architecture.md` as part of this task.

### Frontend work (reuse MT3 design system, ZERO new deps)
- **Persona switcher** in `TopBar.jsx`/`PhoneFrame.jsx` (pill: Buyer · Seller · Ops) OR a small Home launcher in `App.jsx`. Existing inbox = the "Ops" view, untouched.
- NEW screens in `frontend/src/screens/`: `BuyerHome` (browse grid + order-history tabs), `Pdp` (size proof), `SellerDashboard` (return-rate list). REUSE `RadarScreen`/`LiquidityScreen` (resell) and `DiagnoseScreen` (seller why-drilldown).
- `lib/api.js`: add `sizeAdvice(asin)`, `sellerReturns()`, `orders(persona)`; follow the existing `*Safe` cold-start-retry pattern if any new call has a passport prereq (these don't — they're stateless reads).

**Verify (done when):** persona switch reaches all three views with no dead ends; buyer PDP size proof + order-history → one-tap resell → radar/liquidity → ping all work; seller dashboard → tap high-return SKU → diagnose drill-down works; **every new number traces to an endpoint (curl each new route against the Function URL first)**; spine (SL-001) regression-clean; build clean, zero new deps; walked via chrome-devtools on localhost AND the live Vercel URL; new endpoints documented in api-spec.md + architecture.md.

**SEQUENCING:** Build MT7 the **next session** (it's net-new feature scope, and you have runway). Then **MT5** bulletproofs *everything including the MT7 screens* (cached responses for the new AI-touching paths, loading/error states, kill-switch). Then **MT6** submits — and the 3-min beat sheet in docs/demo-and-prfaq.md MAY extend to show one Prevent moment + the Recirculate origin (keep the spine the centrepiece; these are bookends, not replacements). MT7 does not touch the locked spine.

## MT8 — Dark-store node + derived value cascade (A backend + B/Fable frontend) ✅ DONE (commit ddce925, redeployed)
**Result:** all verify checks PASS against the deployed Function URL. **Move 1:** `seed/dark_stores.json` (5 Amazon Now MFCs) + `seed.nearest_dark_store()` (haversine) → `vrs.build_paths` attaches `dark_store {id,name,distance_km}` to an eligible `local_p2p`; web `RouteScreen` winner card shows "Lists open-box at Amazon Now · Indiranagar · 1.9 km". **Move 2:** `cascade.py` + `GET /cascade/{item_id}` re-runs the VRS argmax week-by-week under −5%/wk decay, emits a tier on each channel flip, terminates at `donate` — pure-Python, no AI/cache. Refactored `route_item` into shared `build_paths()` + `grade_for()`; spine math unchanged (local_p2p +₹83 = 125−40−2, all paths reconcile). Frontend `CascadeStrip` below the path list, `api.cascade`/`cascadeSafe`, fetched non-blocking after route; RTO lane gets `cascade=null`. **Verified deployed:** `/cascade/SL-001` 409 pre-grade; post-grade → local_p2p +₹83 (wk0) → donate +₹45 (wk8, terminal), decay 5; `/route` local_p2p carries dark_store DS-09 Indiranagar 1.9 km. Cascade depth is an honest function of the LOCKED constants (shoe = 2 tiers; SL-006 grade C = refurbish→donate). **Move 3 (cross-dock animation) CUT** (STRETCH, from the bottom). Zero new deps, build 299KB. Docs updated to real output. ⚠️ Cascade renders only on the spine RouteScreen in the demo (SL-001); radar/QUEUED-lane items don't reach RouteScreen.

**Why this exists:** the demo proves *recovery* and *recirculation*, but the mental model judges arrive with (from Optoro/ReturnPro, and the returns-recommerce reference doc) is a tiered **waterfall**: "dark store → wholesale → liquidation over time." We already compute that economically — the VRS argmax (which channel) plus −5%/wk time-decay pricing (how value erodes). MT8 composes them into ONE legible artifact, **without touching the spine**. It also gives a visible answer to the standing question *"how are items that DON'T go to a dark store handled?"* — they are the lower tiers the cascade falls through. Decided in the June 13 brainstorm (this session).

**IRON RULE HELD — mock the DATA, not the ARCHITECTURE.** No hardcoded JSX. The dark-store id comes from a NEW `seed/dark_stores.json` via nearest-distance; the cascade is **derived** by re-running the existing VRS engine over a decaying price, never a hardcoded timeline.

**Goal:** RouteScreen names the hyperlocal winner as an Amazon Now dark-store node, and a new cascade strip shows the **derived** tier waterfall (dark-store → wider → wholesale → liquidate → donate) with the real week + price at each step — every figure API-backed, spine (SL-001) regression-clean, on Vercel against the deployed backend.

### Scope — Move 1: name the dark-store node (thin backend + frontend)
- NEW `seed/dark_stores.json` — 4–5 Amazon Now MFCs `{id, name, area, lat, lng}` (the reference doc's "hardcoded map of 4–5 dark stores with lat-long").
- `vrs.py`: when `local_p2p` is eligible, attach `dark_store: {id, name, distance_km}` (nearest MFC to the item's locale) to that path object — see api-spec.md `/route`.
- Frontend `RouteScreen`: render "→ Amazon Now dark store DS-14 · list open-box · ₹X" on the local_p2p winner.

### Scope — Move 2: the derived cascade strip (thin backend + frontend) — THE CENTREPIECE
- NEW `cascade.py` + `GET /cascade/{item_id}` (requires grade; `409` otherwise). Re-runs the VRS argmax week-by-week under −5%/wk decay; emits a tier each time the winning channel flips; terminates at `donate` (CSR). Returns `{tiers: [{week, channel, label, price, net, terminal?}], decay_pct_per_week}` — see api-spec.md. **Pure-Python deterministic → NO cache needed** (can't fail live; nothing for MT5 to cache here). Reuses `vrs.py` + `pricing.py`, no new economics.
- Frontend: NEW cascade-strip component, folded into `RouteScreen` below the path list — horizontal tiers from `/cascade`, each labelled channel + week + price; terminal tier flagged. Richest on the monitor (SL-002, more tiers before terminal); the shoe (SL-001) yields a shorter waterfall — an honest property of the economics, not a bug.
- `lib/api.js`: add `cascade(id)`; if it can hit the cold-start 409, give it a `cascadeSafe` grade-prereq retry mirroring `routeSafe`.

### Scope — Move 3: cross-dock "list-while-in-transit" animation (frontend-only, STRETCH)
- On RouteScreen when local_p2p wins, a CSS-only motion beat: the parcel redirects from reverse-transit to "buyer 4 km away" instead of the 600 km round trip (the reference doc's "winning demo moment"). Zero deps. **Build ONLY if Moves 1–2 land with runway; drop cleanly otherwise.**

### Backend work (thin, seed-backed — redeploy required, like MT7)
- NEW `seed/dark_stores.json`; `dark_store` field on `vrs.py` `local_p2p`; NEW `cascade.py` + `GET /cascade/{item_id}`; register router in `app/main.py`; **redeploy via `backend/deploy.ps1`** (Docker up). `/cascade` + the `/route` `dark_store` field are already documented in docs/api-spec.md and docs/architecture.md §9 (written with this spec).

### Frontend work (reuse MT3/MT4 design system, ZERO new deps)
- `RouteScreen` dark-store label + cascade strip; optional cross-dock animation; `lib/api.js` `cascade(id)`. Reuses the existing palette, motion, and `ui` primitives.

**Verify (done when):** `GET /cascade/{id}` curled against the Function URL returns a tier timeline whose `channel`/`net` at each week match the VRS argmax at that decayed price (derived, NOT fixed); `/route` local_p2p carries a `dark_store` from `dark_stores.json`; RouteScreen shows the dark-store label + cascade strip with every figure traceable to an endpoint (network tab clean); spine (SL-001) regression-clean; build clean, zero new deps; walked via chrome-devtools on localhost AND the live Vercel URL; `/cascade` + `dark_store` documented in api-spec.md + architecture.md.

**UI NOTE (added with MT9, 2026-06-13):** MT8 now runs **after MT9 (web console redesign)**. The phone frame is gone — MT8's dark-store label + derived cascade strip render in the **web** `RouteScreen`, not a 390px phone column. Scope is otherwise unchanged (derived cascade via VRS argmax over −5%/wk decay; `seed/dark_stores.json`; `GET /cascade/{item_id}`). Read docs/superpowers/specs/2026-06-13-web-console-redesign-design.md before building MT8 so you target the right components.

**SEQUENCING:** Build **MT9 first** (de-phone the demo), THEN MT8 (cascade into the web RouteScreen — net-new, additive, does NOT touch the spine). Then **MT5** bulletproofs everything incl. MT8's loading/error states (no AI cache for `/cascade` — it's deterministic). Then **MT6** submits; the beat sheet MAY add the cascade as the "terminal-state" close right after the VRS reveal (spine stays the centrepiece). MT8 is numbered after MT7 but runs after MT9, before MT5/MT6.

## MT9 — Web console redesign: de-phone the demo (B frontend + A thin backend) ✅ DONE (this session)
**Result:** all verify checks PASS, walked end-to-end via chrome-devtools on localhost against the **redeployed** backend (prod verify on push). Phone frame deleted; `WebShell` (Amazon-navy brand bar + `max-w-6xl` column) + light `TopBar` page header + de-phoned `.screen-scroll`/`.screen-page`. Home → web landing grid; `ItemIntro` → **two-pane scan station** with a real upload dropzone (`lib/image.js` canvas downscale → base64); new `BuyerStore` (Shop/Cart/Orders/Notifications) + `Checkout` (UPI) replace `BuyerHome`; `SellerDashboard` → wide return-rate table; spine/radar/liquidity/diagnose/health-card/metrics reused + widened, **logic untouched**. Backend: `/grade` `current_images` (≤3, ≤1.5 MB, 422 oversize/invalid, `graded_uploaded_photos` flag); `buyer.py` + `seed/buyer.json` → `/cart` (GET/POST), `/notifications`, `/checkout` (demo UPI pending→success, empties cart). Redeployed via deploy.ps1. **Verified:** uploaded SL-001 photo → `/grade` body had `current_images` → **live-bedrock** grade of the uploaded photo (`graded_uploaded_photos:true`) → spine reconciles (+₹83) → Health Card → radar ping; Shop→PDP fit proof→add-to-cart(server-merged)→UPI pending→success(order/amount preserved across confirm); Orders→resell→radar; Notifications→resell; Seller table→diagnose. **0 console errors, zero new deps, build 300 KB.** Fixed: confirm preserves the approved order id/amount (cart is per-instance). Endpoints + `/grade` field documented in api-spec.md + architecture.md §10.

**Why this exists:** the demo works but is hard to read on stage — everything lives inside a single 390px **phone frame** and the navigation is dense. MT9 is a **presentation change, not a feature change**: re-house the existing console as a real **web** experience so a judge instantly understands it. Almost every feature already exists (the grade→route→health-card→radar spine, the size-proof PDP, the seller return dashboard, the resell→radar flow); MT9 reskins them as web pages and adds two genuinely new pieces — **photo upload on the grading page** and a fleshed-out **buyer storefront**. Decided + spec'd in the 2026-06-13 brainstorm. **FULL SPEC: docs/superpowers/specs/2026-06-13-web-console-redesign-design.md (read it first).**

**Decisions locked:** (1) UI first, MT8 after; (2) **hybrid photo upload** — grade the actual uploaded current photos live (Nova→Gemini→cached floor), no upload → seeded current photos; (3) **all API-backed** — cart/notifications/orders/checkout via thin seed-JSON endpoints (iron rule holds); (4) one MT9, spine protected, edges cut from the bottom.

**Approach — EVOLVE IN PLACE, do not rebuild.** Swap `PhoneFrame` for a `WebShell`, widen each screen from the fixed 390px column to a responsive page, add the buyer-storefront areas + the grading two-pane. **Keep** `App.jsx` state machine, `lib/api.js`, the Tailwind `@theme` tokens, the CSS animations, and ALL backend logic. **Zero new deps.** ⭐ Spine (SL-001) must stay regression-clean.

### Scope (build order — spine first)
1. **Shell + homepage** — new `WebShell` (Amazon-navy top bar over a centered `max-w-6xl` column); retire `.phone`/`.phone-screen`. Homepage = the **existing `Home.jsx` three-card concept, de-phoned** (Returns desk = start here · Rahul/Buyer · Vastram/Seller + the Prevent·Recover·Recirculate band), re-laid as a web landing. No new homepage concept.
2. **Returns desk (AI-grading + spine)** — `ItemIntro` → **two-pane**: LEFT catalog + day-0 thumbnails (baseline), RIGHT **upload dropzone** + current-photo thumbs + "Run AI grade". Downstream `Grade` / `RouteScreen` / `HealthCard` / `RadarScreen` / `LiquidityScreen` / `SealLane` / `DiagnoseScreen` / `MetricsScreen` reused + widened, **logic untouched** (the protected spine; the side-by-side grade view gains room on web).
3. **Buyer storefront (Rahul)** — Shop (grid→PDP, reuse `Pdp`) · Cart (`GET /cart/rahul`, seeded lines in the recommended size) · Orders (`/orders/rahul`, Return/Resell→existing radar flow) · Notifications (`GET /notifications/rahul`, hero "baby monitor — N buyers nearby, resell?"→radar). Buy → **UPI checkout** (`POST /checkout/rahul` → "Approve the payment in your UPI app" → success), API-returned.
4. **Seller dashboard (Vastram)** — `SellerDashboard` → wide **return-rate table** (worst-first) from `/seller/returns`; tap SKU → `DiagnoseScreen` drill-down (`/diagnose-listing`). Web table, same data.
5. **UI/UX polish** — buttons/hover/page transitions where they read better on web (never at the spine's expense).

### Backend work (thin, seed-backed — REDEPLOY required, like MT7/MT8)
- `/grade` accepts optional `current_images: list[str]` (base64; ≤3 imgs, ≤~1.5 MB each decoded; frontend downscales to ~1024px/~300 KB first; oversize → 422). `grading.grade_item(..., current_images=None)` uses them as the **current** set (catalog+day-0 still from seed), else seeded current. Hybrid fallback unchanged.
- NEW `buyer.py` + `seed/buyer.json`: `GET /cart/{persona}`, `POST /cart/{persona}` (in-memory append, per-instance like the passport), `GET /notifications/{persona}`, `POST /checkout/{persona}` (`{order_id, amount, upi_vpa, status}`; pending→success).
- Register routers in `app/main.py`; redeploy via `backend/deploy.ps1` (Docker up). **Document the new endpoints + the `/grade` `current_images` field in docs/api-spec.md and docs/architecture.md as part of the build.**

### Frontend work (reuse MT3 design system, ZERO new deps)
- New `WebShell`; `Home` → web landing; `ItemIntro` → two-pane + upload; new `BuyerStore` + `Checkout`; `SellerDashboard` → table; reuse the rest. `lib/api.js`: `grade(id, forceCached, currentImages)`, `cart`, `addToCart`, `notifications`, `checkout`; keep the `*Safe` cold-start wrappers.

**Verify (done when):** homepage (3 options) → all three sections reachable, no dead ends, on the live Vercel URL against the deployed backend; returns desk: **upload current photos → live grade of the UPLOADED photos** (network tab shows `current_images` in the `/grade` body) → route → health card → radar ping, AND no-upload seeded grade still works, **spine (SL-001) regression-clean**; buyer: Shop→PDP size proof→add to cart→cart line→UPI checkout→success, Orders resell→radar/liquidity→ping, Notifications nudge→resell; seller table→tap SKU→diagnose; every new number traces to an endpoint (curl each first); build clean, zero new deps; walked via chrome-devtools on localhost AND the live Vercel URL; new endpoints + `/grade` field documented in api-spec.md + architecture.md.

**SEQUENCING:** Build MT9 the **next session** (it's the de-phone reshell — do it before MT8 so the cascade strip is built into the web RouteScreen, not the phone frame). Then MT8 (cascade), then MT5 (bulletproof, now incl. MT9's upload + storefront loading/error states + checkout), then MT6 (submit).

## MT10 — Buyer-flow fixes + resell redesign (A backend + B frontend) ✅ DONE (this session)
**Why this exists:** five buyer/ops/resell defects found while reviewing the demo, fixed before MT5. Spec: docs/superpowers/specs/2026-06-13-mt10-buyer-flow-fixes-resell-redesign-design.md · Plan: docs/superpowers/plans/2026-06-13-mt10-buyer-flow-fixes-resell-redesign.md.
**Result:** all backend verify checks PASS on the deployed Function URL; frontend builds clean (317 KB, zero new deps) and is pushed to Vercel. Browser walkthrough left to the user (per their instruction).
- **Fix 1 — Return window:** `orders.py` adds `return_window_open`/`return_by`/`days_left` (10-day window vs demo date 2026-06-13); +2 recent Rahul orders so it's demoable. `BuyerStore` Orders gates the Return button.
- **Fix 2 — Ops desk de-mix + buyer→ops link:** NEW `returns.py` + `seed/returns_seed.json` + `GET/POST /returns` (per-instance store). `Inbox` redesigned to two sections (Returns to process · COD refused/RTO); idle/diagnose lanes moved out. Buyer Return `POST /returns` → row shows on Ops. Hero SL-001 stays the only live-gradeable row; extras display-only.
- **Fix 3 — Personalized size:** NEW `seed/purchase_profile.json` + `size_advice(asin, persona)` adds a `personal` block matched by brand/category; `/size-advice/{asin}?persona=`; PDP renders the green "From your purchases" card.
- **Fix 4 — Resell redesign:** NEW `resell.py` + `/resell/quote` (range→reachable→demand→best_price; `delivery_cut = 25 + 6·km`; net peaks mid-range) + `/resell/listings` + live `/interest`. Flow: Orders→Resell → `ResellConfirm` (bought price/date) → photo upload→AI grade→price → `ResellPrice` (price slider + 3/7/15 km range, net trade-off) → list → `FlashDeals` board (any tab, "I'm interested") → `MyResells` live feed (polls, real cross-tab). Old SL-002 notification radar-resell beat untouched.
**Verify (done):** every new endpoint curled on the Function URL (`/returns`, `/orders` window fields, `/size-advice?persona`, `/resell/quote|listings|interest`); spine SL-001 grade D regression-clean; build clean; docs (api-spec §, architecture §11) updated; backend redeployed (ECR cred-helper fix — see lessons.md), frontend pushed. ⚠️ Per-instance stores (returns/listings/interests) — fine for demo, note for MT5.

## MT10.1 — Resell sell-to-buyer + real-time inspectable returns + one resell path (A backend + B frontend) ✅ DONE (June 14)
**Why this exists:** the user reported three MT10 flows broken: (1) after a resell is listed and a buyer taps "interested", the reseller had NO way to accept/decline or one-tap sell — `MyResells` was a read-only feed; (2) returning the kurta from Orders did not update the Ops Returns desk in real time, and returned items weren't inspectable/gradeable; (3) the notification "Resell now" took a different path (old radar flow) than order-history resell. Plus a sweep for related issues.
**Result:** all three fixed and verified in-browser via chrome-devtools (localhost → deployed backend, 0 console errors).
- **Fix 1 — sell-to-buyer:** `resell.py` listings gained `status`/`sold_to`/`net_earned`; `sell_to_interest`/`decline_interest`; `POST /resell/listings/{id}/sell|/decline`. `MyResells` shows each interested buyer with their offer + your take-home, Sell/Decline buttons, and a SOLD summary (₹net credited); polling pauses during an action. `FlashDeals` hides "I'm interested" on your own listings and shows SOLD ones as "Sold nearby".
- **Fix 2 — real-time + gradeable returns:** `returns.py` entries carry `item_id`; App keeps a session-local `localReturns` merged ahead of the server `/returns` (dedup by order_id, local wins) so a buyer return shows on the Ops desk instantly and survives a cross-instance refetch. Return rows with an `item_id` render as a clickable `GradeableReturn` ("Inspect & grade") → opens the SAME spine as the hero via `openReturnForGrading` (forces `lane="spine"`). NEW `seed/cached/SL-003.grade.json` so the kurta grades B in live AND cached mode.
- **Fix 3 — one resell path:** `openNotif` now calls `startResell` (the confirm→photo→price→list flow), looking up the matching order for real paid/date; orphaned `resellOrder` deleted. The Idle Asset Radar beat stays reachable from the Ops inbox SL-002 lane.
**Verify (done):** new `/sell`/`/decline` + SL-003 cached grade curled live; full UI walkthrough — notification→resell→list→sell, return→Ops desk→grade→route — passes; build 322 KB, zero new deps; backend redeployed (digest confirmed via curl), frontend pushed. ⚠️ Per-instance stores unchanged (note for MT5).

## MT11 — Audit follow-ups (end-to-end audit, June 14) ✅ DONE (this session, redeployed digest `sha256:7b905d33…`)
**Why this exists:** a full end-to-end pre-MT5 audit (correctness · demo reliability · UX · deploy · judge perception). The **blocker + both HIGH findings were FIXED + SHIPPED** earlier (commit `cf5222e`) — see the ✅ context block below. MT11 was the **remaining can-wait backlog**: none demo-blocking, each a real polish/correctness gap. **Iron rules held:** SL-001 spine protected (still `local_p2p +₹83` vs `warehouse −₹129`, verified live), every on-screen number API-backed.
**Result:** all items fixed-and-verified or explicitly deferred. **LOW 8** (orders `TODAY` now derived from the latest seeded order — never goes stale), **LOW 9** (already resolved by the photo commit — re-verified), **LOW 10** (D grade-label → "Heavily used"), **LOW 11** (NEW `backend/tests/test_vrs.py` — `recovery == sum(breakdown)` for every eligible path × all items × grades A–D + the SL-001 +83/−129 golden; passes), **MED 5** (demo-script metrics narration reconciled to the real `/metrics` baseline + NEW `POST /metrics/reset` presenter tool for a stable stage number) all DONE; **MED 6** (per-instance resell cross-tab) DEFERRED to a durable DynamoDB backing (off-spine; demo-safe practice documented); **NEW buyer-side `/second-life/{asin}` PDP row** BUILT (price from `pricing.resale_value`, grade/distance/eta seeded; row on `Pdp.jsx`, tap → reservation toast). Build 328 KB, zero new deps. Backend redeployed + re-curled; frontend pushed. **Verified live** (curl: `/second-life` shoe C ₹238 / D ₹132, monitor empty, bogus 404; `/orders/rahul` windows open; `/metrics` baseline + reset cycle; spine SL-001 +83/−129) **AND in-browser** (chrome-devtools localhost → deployed backend: PDP Second Life row renders, tap → toast, 0 console errors).

### ✅ Already fixed + shipped this session (do NOT redo — context only)
- **🔴 BLOCKER — hero grade.** SL-001 graded as "a completely different product (Nike) → manual review", contradicting the F1/F3 pitch on stage. Root cause = demo DATA (seeded current photo was a Nike against a North Star day-0 baseline), not the pipeline. Fixed: real same-shoe photos swapped into `backend/app/seed/images/SL-001/` + `frontend/public/items/SL-001/`, re-captured `cached/SL-001.grade.json` → `same_unit.verified:true`, honest **grade D** (4 real defects), `needs_human_review:false`. Spine math unchanged (local **+₹83** vs warehouse **−₹129**). **Verified live** on the Function URL (cached + live-gemini) AND on Vercel (new image `content-length 49503`).
- **🟠 HIGH 2 — number drift.** PRD §5 table + §4 inline + demo script said `+₹290 / −₹70`; the live engine gives `+₹83 / −₹129` (grade D) and `+₹279 / +₹66` (grade B) — never +₹290/−₹70. Reconciled `docs/PRD.md` + `docs/demo-and-prfaq.md` to the real numbers; warranty "8 months" → real figure.
- **🟠 HIGH 3 — provider story.** Running system is **Gemini 2.5 Flash primary / Bedrock Nova failover**, but CLAUDE.md + PRD §6 + demo narration claimed "Bedrock-primary." Decision: **keep Gemini-primary** (free-tier headroom), make docs honest. Reconciled `CLAUDE.md`, `docs/PRD.md §6`, `docs/architecture.md` (subgraph label, §3 heading, failover-direction row), and the demo narration + a new Q&A #9 (provider-agnostic, Nova = AWS-native failover).

### Remaining backlog (severity · file:line · fix · verify)
- **🟡 MED 5 — batch-metrics narration ≠ live `/metrics`, and the counter drifts.** `docs/demo-and-prfaq.md` beat 2:35 narrates "8 items → ₹4,830 recovered vs ₹0"; `backend/app/metrics.py:11-20` BASELINE is 5 items / ₹3,120 / 80%, and `metrics.py:28-56` is cumulative over the warm Lambda — every rehearsal route bumps it permanently. *Fix:* reconcile the script to the real `/metrics` shape, OR add a reset / present the metrics beat on a cold instance (or behind `FORCE_CACHED`) for a stable stage number. *Verify:* the number narrated == `/metrics` on the instance you'll demo on, stable across two runs.
- **🟡 MED 6 — resell two-person cross-tab beat is per-instance in-memory → can silently dead-end.** `backend/app/resell.py:36` (`_LISTINGS` global), `returns.py:15-24`, `buyer.py:17`; `MyResells.jsx`/`FlashDeals.jsx` poll `/resell/listings`. Two browsers on different warm instances (or a cold start) lose the listing/interest. *Fix (demo-safe):* keep both tabs on one pre-warmed instance, don't idle >~5 min, rehearse cold once. *Fix (durable):* back listings/returns/cart with the DynamoDB table already wired for the passport. Keep this beat OFF the 3-min spine. *Verify:* list on tab A → appears on tab B → sell → SOLD, including after a deliberate cold start.
- **🟢 LOW 8 — hardcoded demo "today" is stale.** `backend/app/orders.py:17` `TODAY = date(2026, 6, 13)` (actual date is later). Return windows still open today, but if the demo slips a few days the buyer "Return" button vanishes (window closes). *Fix:* bump `TODAY` near demo day (or derive from a fixed offset off the latest seeded order). *Verify:* `/orders/rahul` → `return_window_open:true` with `days_left>0` on the 2 recent orders on demo day.
- **🟢 LOW 9 — Buyer Orders thumbnail hardcodes `category="electronics"`.** `frontend/src/screens/BuyerStore.jsx:148` builds `/items/{item_id||"x"}/current_1.jpg` with `category="electronics"`; orders with no catalog item (stroller/crib) 404 → wrong fallback glyph; the kurta order gets an electronics glyph if its image 404s. *Fix:* pass `o.category`; sensible default. *Verify:* Orders tab shows correct category tiles for non-electronics/imageless orders.
- **🟢 LOW 10 — grade-label copy vs rubric.** `frontend/src/lib/format.js:30` maps D→"Used — fair", but the RUBRIC (`backend/app/grading.py:39-46`) defines D as "damaged, torn, broken." On the hero (heavy wear) "Used — fair" reads generous next to "extensive dirt, scuffs, wear." *Fix:* tighten the D label (e.g. "Heavily used"). *Verify:* Grade screen label matches the defect severity shown.
- **🟢 LOW 11 — no test guards the VRS reconciliation invariant.** The property the whole pitch leans on — each path's `recovery == sum(breakdown)` (`backend/app/vrs.py:47-62`) — has no test. Not demo-blocking; a constant edit could silently break the on-screen math. *Fix:* one pytest (or an assert in a `scripts/` check) asserting `recovery == sum(breakdown.values())` for every eligible path across all seed items at grades A–D, plus the SL-001 hero `+₹83 / −₹129` golden. *Verify:* the test passes now and fails if a VRS constant is nudged.
- **🟡 NEW — Buyer-side "Second Life options near you" PDP row (B + thin A).** The one real *build* gap vs the strategic review: the whole solution is supply-side (grade/route/resell/list); the buyer never *meets* a recovered unit on a normal product page. This is the buy-side twin of the "layer, not app" thesis and the part Renewed gets wrong (separate storefront nobody visits). Small, high demo-value, closes the loop. Distinct from the size-proof block + the lite "resells for ~₹X near you later" hint already in `Pdp.jsx` — this is the *buy-side* row. **IRON RULE HOLDS — mock the DATA, not the ARCHITECTURE** (grade/price/distance/ETA come from a real endpoint, never hardcoded JSX). *Scope:* (backend, thin/seed-backed, redeploy) NEW `GET /second-life/{asin}` → 1–2 nearby recovered units `{grade, price, distance_km, eta, item_id}`, reusing `pricing.resale_value` (price) + `radar.py`/`neighbors` (distance) + the seeded grade; back it with a tiny `seed/second_life_offers.json` or derive from existing seed items; register in `app/main.py`; document in `docs/api-spec.md` + `architecture.md`. (frontend, reuse design system, zero new deps) a row under the buy box on `Pdp.jsx` — *"Second Life options near you: Grade A · ₹310 · 4 km · delivery today"* — via new `api.secondLife(asin)`; tap → reuse an existing screen (Health Card or the buyer flow), no new screen. *Verify:* row renders on SL-001/SL-003 PDP with every figure traced to `/second-life/{asin}` (network tab clean, no hardcoded JSX); spine (SL-001) regression-clean; build clean, zero new deps; walked via chrome-devtools on localhost AND the live Vercel URL; endpoint documented. **Additive bookend, NOT the spine — cut from the bottom if MT11 runs out of runway.**

**Verify (MT11 done when):** each remaining item above is either fixed-and-verified or explicitly deferred with a one-line reason in STATE.md; spine (SL-001) regression-clean; build clean, zero new deps; if any backend file changes, redeploy via `deploy.ps1` and re-curl the Function URL (never trust "Done. Deployed" — lessons.md). The full audit (risk map, ranked findings, must-fix vs can-wait) was delivered in the June 14 session — this MT is its persisted backlog. ✅ **MT11 DONE** (incl. the buyer-side PDP "Second Life options near you" row).

## MT12 — Playbook-gap closures + iron-rule fixes (rolling backlog) ✅ DONE (this session — all 12 items, redeployed digest `sha256:f07f8a8c…`)
**Result:** all 12 sub-items shipped in one session (the user said "do everything"). Backend redeployed via `deploy.ps1` and the new endpoints curled on the deployed Function URL; frontend builds clean (336 KB, **zero new deps**) and is pushed. **Spine regression-clean** on the live Function URL (`local_p2p +₹83` vs `warehouse −₹129`). New backend: `lifestage.py` + `seed/lifestage_curves.json` + `GET /life-stage/{asin}?persona=` (NEW 1); `catalog_matches_day0` + `fault_attribution`/`returnable` on `/grade` + updated cached grades (NEW 10); `resell.py` listings carry `grade`/`confidence`/`source` + `POST /resell/from-route/{id}` (NEW 9/12); orders.json + purchase_profile.json reconciled with distinct brands (NEW 2/3); `buyer.py` renders the resell notification from the life-stage payload (NEW 1). New/changed frontend: `FlashDealDetail.jsx` (NEW 12), `RouteScreen` destination-hero redesign (NEW 8), `ItemIntro` require-upload + seller-captured copy (NEW 5/6), `Grade` uploaded-photo NOW tile + fault banner + "Prediction confidence" (NEW 6/7/10), `BuyerStore` Return/Replace/Resell (NEW 4), `ResellConfirm` require-upload + App-side mismatch block (NEW 11), `FlashDeals` card→detail + Returned/Grade badge (NEW 9/12). Verified live (curl): `/life-stage/B0MONITOR1` → 19mo/18 typical/₹978/₹68 per month/due; `/orders/rahul` lists the Linen Shirt + Stride Sneakers; `/grade SL-001` → fault `none`, returnable, `catalog_matches_day0` present; `/resell/from-route/SL-001` → `source:return grade D ask ₹132`. api-spec.md + architecture.md §12 updated. **In-browser walkthrough left to the user** (their instruction). ⚠️ Per-instance store caveat unchanged (listings/returns reset on cold start). ⚠️ Seller-fault path (catalog≠day0) is implemented + demoable only if catalog≠day-0 photos are supplied; customer-fault is demoable live (upload a different item). Original backlog spec retained below for reference.

**Why this exists:** a running list of smaller gaps surfaced by the playbook + strategic-review cross-check (the session after MT11). None is demo-blocking; each is a real correctness/honesty/depth gap a careful judge could probe. Same discipline as MT11 — fold into MT5's bulletproof pass or knock out independently. **Iron rules hold:** protect the SL-001 spine; every on-screen number stays API-backed (the recurring theme below is *retiring hardcoded copy in favour of derived numbers*). Append new gaps here as they're found.

### Backlog (severity · what · fix · verify)
- **🟡 NEW 1 — Life-stage prediction is flavor text, not a mechanism (and it's hardcoded → breaks the iron rule).** The hero notification in `backend/app/seed/buyer.json` (`rahul.notifications[0]`) reads *"Idle 19 months in your home — 3 buyers searching 'baby monitor' within 4 km"* — but the actual trigger is the demand radar, the "19 months" + "₹X dropping/month" are **static seed strings** (served read-only by `buyer.py:get_notifications`, no computation), and there is **no per-category lifecycle curve** anywhere. So it *looks like* the playbook's life-stage prediction (§6.3 ★) but is neither time-triggered nor derived. *What it actually should be:* products end a life-*stage* on a predictable curve (baby gear ~18mo, textbooks ~1 semester, phones ~2yr) → ping the owner **because the time elapsed**, with a forward value-decay projection ("current value ₹2,100, dropping ~₹150/month"), *before* any buyer searches — the time-based twin of the demand-based radar. *Fix (cheap, ~30 min, keeps the iron rule):* a tiny `life_stage(asin, purchase_date)` (new `lifestage.py` or fold into `radar.py`) reading a per-category lifecycle curve from a small seed JSON (e.g. `seed/lifestage_curves.json`: category → typical-life-months + ₹/month decay) + the owner's purchase date from `orders.json`; expose via `GET /life-stage/{asin}?persona=` returning `{months_owned, stage_pct, current_value, decay_per_month, due_to_resell}`; have the notification copy render from that payload instead of the hardcoded string. Register in `app/main.py`; document in `docs/api-spec.md` + `architecture.md`. *Verify:* the monitor notification's "months owned" + "₹/month" trace to `/life-stage/B0MONITOR1?persona=rahul` (network tab, no hardcoded copy); spine (SL-001) regression-clean; build clean, zero new deps; walked via chrome-devtools on localhost AND live Vercel. **Additive — cut from the bottom if MT12 runs out of runway; if cut, DON'T claim "predictive lifecycle curve" on stage (the honest answer would be "seeded copy").**

- **🟡 NEW 2 — Order history must show the SAME past products used in the size recommendation (consistency).** The personalized size card (`backend/app/size.py` `_personal()` over `seed/purchase_profile.json`) cites *"You bought a Vastram Linen Shirt (M)"* + *"Aurelle Trail Sneakers (UK 8)"*, but `seed/orders.json → rahul_order_history` (the Orders tab, via `GET /orders/rahul`) is a DIFFERENT set (NestCam monitor, GlidePro stroller, DreamNest crib, Vastram kurta, Aurelle shoes) — so "you earlier bought this" has no matching order row. *Fix:* reconcile the two seeds — add the Linen Shirt + Trail Sneakers as real rows in `rahul_order_history` (older `purchase_date`; return-window-closed is fine), OR drive `_personal()` off `order_history` directly so they can't diverge. Keep brand/category aligned with `seed/size_signals.json`. *Verify:* every product named in the PDP personal size card also appears as a row in the Orders tab; `/orders/rahul` ↔ `/size-advice/{asin}?persona=rahul` reference the same titles.
- **🟢 NEW 3 — Editable company/brand names in the size recommendation.** Brands are derived from the first word of titles (`size.py:_brand`) over `seed/purchase_profile.json` + the catalog item titles + `seed/size_signals.json`. To make the cross-brand sizing story land ("in Brand A you're 8, in Brand B 8.5"), edit those seed titles/brand fields consistently. *Fix:* pure seed copy edit across `purchase_profile.json` (brand+title), the catalog item title (`seed/items*.json`), and `size_signals.json`; no logic change. *Verify:* PDP personal card + fit-proof block read with the new names; spine unaffected.
- **🟡 NEW 4 — Order history: THREE actions (Return · Replace · Resell); don't flip the button to "Done"; REPLACE = "pickup arranged".** `frontend/src/screens/BuyerStore.jsx` Orders (≈lines 155-182) currently shows two buttons — "Return or replace · Nd left" (`onReturn`→`returnOrder` App.jsx:460) and "Resell on Second Life" (`onResell`→`startResell`). *Desired:* split into THREE distinct buttons:
  - **Return** → keep the EXISTING returns-desk flow (`returnOrder` → `POST /returns` → Ops desk → grade). Unchanged.
  - **Replace** → NEW, simple: just show a **"Pickup arranged"** toast/confirmation (the old unit is collected, replacement on the way). No grading, no pricing screen. This is the new action.
  - **Resell** → keep the EXISTING FULL flow (`startResell` App.jsx:494 → confirm → photo → AI price → price/range → list on Flash deals). Unchanged.
  After tapping **Return** or **Replace**, do NOT switch the button to a "Done"/disabled state — keep it actionable or show a subtle status chip (the toast already fires). (Clarified by user: the "pickup arranged" message is for **Replace**, NOT Resell — both Return and Resell flows stay exactly as they are.) *Verify:* Orders rows show Return/Replace/Resell; Return posts to the returns desk and the button stays usable (no "Done"); Replace shows "Pickup arranged" and stays usable; Resell still opens the full confirm→photo→price→list flow.
- **🟢 NEW 5 — Returns-desk copy: "photos clicked on delivered date" → "photos clicked by seller".** `frontend/src/screens/ItemIntro.jsx:82-85` reads "Catalog + the photos captured **the day it was delivered**…" — reframe the day-0 baseline as **seller-captured** (at pack-out/listing): e.g. "…the photos **clicked by the seller** at listing." (Ties conceptually to NEW 10 — day-0 is the seller's baseline.) Grep the returns-desk screens (`ItemIntro.jsx`, `Grade.jsx`) for other "delivered"/"day-0 = delivery" phrasings and align. *Verify:* scan station + grade screen describe day-0 as seller-captured; no "delivered date" wording remains.
- **🟡 NEW 6 — Returns desk: REQUIRE an upload (no seeded default); show the UPLOADED photos on the grade screen.** Today `ItemIntro.jsx` lets you proceed with NO upload (falls back to seeded `current_1.jpg`; copy at lines 177-181/188-191), and `Grade.jsx` CompareTile "NOW" (line 55) always renders seeded `/items/{id}/current_1.jpg`, not the upload. *Desired:* (a) current-condition pane starts BLANK; (b) "Run AI grade" disabled until ≥1 photo uploaded (remove the no-upload path + copy); (c) thread the uploaded previews to `Grade.jsx` so the NOW tile + before/after show the UPLOADED photos (App.jsx pass `images` previews alongside the grade result; Grade renders them instead of `current_1.jpg`). Backend already grades uploads (`graded_uploaded_photos`). *Verify:* cannot grade without uploading; grade screen NOW image = the uploaded one; spine (SL-001) still reconciles (+₹83).
- **🟢 NEW 7 — Confidence label clarity → "Prediction confidence".** `frontend/src/screens/Grade.jsx` ConfidenceRing label is "conf." (line 170) — reword to "Prediction confidence" (relabel the ring/headline); keep the same `grade.confidence` value. *Verify:* grade screen reads "Prediction confidence · NN%".
- **🟠 NEW 8 — Redesign the Recovery-Path screen to show clearly WHERE the item goes + the difference.** `frontend/src/screens/RouteScreen.jsx` works but reads as a list; make the winner destination + the local-vs-warehouse contrast the hero. *Goals:* (a) the chosen destination unmistakable — where it physically goes (local buyer / Amazon Now dark store / donate), ideally a "600 km warehouse → 4 km local" visual; (b) the **₹ swing + km saved** as the headline difference (already computed: `swing`, `route.km_saved`, `route.co2_saved_kg`); (c) keep ALL figures API-backed (no hardcoded JSX), spine math unchanged, cascade strip retained. Use the design skills (frontend-design / ui-ux-pro-max / high-end-visual-design). *Verify:* a judge glance reads where the item goes and why local wins; numbers still trace to `/route` + `/cascade`; build clean, zero new deps.
- **🟡 NEW 9 — Show graded RETURNS on Flash deals (alongside resells) once they route to local_p2p.** `frontend/src/screens/FlashDeals.jsx` currently lists only `/resell/listings`. *Desired:* when a returned item is graded on the Ops desk and its VRS winner is `local_p2p`, it also appears on the Flash-deals board. *Fix:* on `/route` (or a follow-up) when `winner.path == "local_p2p"`, create a board entry (reuse `resell.create_listing` with `owner`="Amazon · returned"/the seller, `source:"return"`, carrying grade+price from the route) so `FlashDeals` renders it generically; tag with a "Returned · Grade X" badge to distinguish from neighbour resells. Pairs with NEW 12. *Verify:* grade a return → route local_p2p → shows on Flash deals with a returned badge; resells still show; per-instance store caveat (cold start resets) noted.
- **🟠 NEW 10 — Grading fault attribution: seller-fault vs customer-fault (+ block return + human review).** Today `backend/app/grading.py` returns a single `same_unit` (current vs catalog/day-0 jointly). *Desired two-way check:* (1) **CATALOG ≠ DAY-0** → **seller's fault** (listed wrong/mismatched photos); (2) catalog == day-0 but **buyer's uploaded CURRENT ≠ day-0** → **customer's fault**, mark **"cannot be returned"** AND set `needs_human_review`. *Fix:* extend the grade schema + `_build_prompt` to return BOTH comparisons — add `catalog_matches_day0 {verified, confidence}`, keep `same_unit` as day0-vs-current; in `grade_item` derive `fault_attribution` ∈ {none, seller, customer} + `returnable` bool feeding the trust gate (customer-fault ⇒ not returnable + review). Surface a clear seller/customer banner in `Grade.jsx`. Update cached JSONs (`seed/cached/SL-001.grade.json`, `SL-003.grade.json`) to the new shape so cached mode matches live. *Verify:* catalog≠day0 demo → "seller's fault"; day0≠uploaded demo → "customer's fault · cannot be returned" + review flag; normal SL-001 → fault none, grade D, spine reconciles.
- **🟡 NEW 11 — Resell must reuse the day-0/catalog grade flow and BLOCK a mismatched item.** `resellGradePhotos` (App.jsx:517) already grades uploads against catalog+day-0 (good), but `ResellConfirm.jsx` lets you proceed with NO upload ("grade the photos on file at a like-new floor", lines 116-121) and advances to pricing regardless of identity. *Desired:* (a) require ≥1 upload in `ResellConfirm` (remove the no-upload floor + copy + the `setStage("photos")` skip); (b) after grading, if `same_unit.verified` is false / customer-fault (NEW 10) → do NOT advance to `resellPrice`; show "this doesn't look like the item you bought" and stop; (c) show the uploaded photos downstream (ties to NEW 6). *Verify:* uploading a different item on resell blocks listing with a clear message; a genuine unit prices + lists as today.
- **🟡 NEW 12 — Flash-deals: tap a product → condition photos + AI grade + confidence → THEN express interest.** `FlashDeals.jsx` "I'm interested" is currently a direct button on the card. *Desired:* tapping a card opens a detail view showing the reseller/returned **current-condition photos**, the **AI grade + confidence %** (from those uploaded photos), then "I'm interested". *Fix:* persist the grade summary (+ photo refs) on the listing — extend `resell.create_listing` (and the NEW 9 return path) to store `grade`, `confidence`, `condition_photos` (b64 previews in-memory, per-instance; or store the grade payload + reuse seeded photos if upload persistence is too heavy for the demo); `get_listing` returns them; NEW `FlashDealDetail` screen renders photos + grade + confidence + interest. *Verify:* tap a flash deal → see its condition photos + AI grade + confidence → "I'm interested" posts as today; works for both resells and graded returns (NEW 9).

**Verify (MT12 done when):** each item above is either fixed-and-verified or explicitly deferred with a one-line reason in STATE.md; spine (SL-001) regression-clean (`local_p2p +₹83` vs `warehouse −₹129`); build clean, zero new deps; any backend change redeployed via `deploy.ps1` and re-curled on the Function URL (never trust "Done. Deployed" — lessons.md); UI walked via chrome-devtools on localhost AND the live Vercel URL. **Suggested grouping for one session:** NEW 5/6/7 (returns-desk copy + upload-required + label) together; NEW 10/11 (fault attribution + resell identity block) together since both touch grading identity; NEW 9/12 (returns-on-flash + flash detail) together; NEW 2/3 (seed reconcile) trivial; NEW 4 + NEW 8 are the bigger standalone UX pieces.

## MT13 — Frontend/UI redesign: whitish, Amazon-relatable reskin (identity preserved) (B + Fable, design skills) ✅ DONE (this session, frontend-only, verified in-browser)
**Result:** all verify checks PASS, walked via chrome-devtools on localhost against the deployed backend (prod verify on push). Presentation-only reskin (like MT9's de-phone): the warm-cream canvas (`--color-sl-paper #f6f3ec`) retuned to a cool near-white (`#f4f8f6`, whisper emerald) + NEW `.aurora-bg`/`.aurora-grain` (emerald corner-pools ~3-4% + ~2% film grain) replacing the dark `.stage-bg`/`.stage-grain`; `--color-sl-line`/`--color-sl-muted`/`--color-sl-ink` cooled (muted bumped to `#5b6964`, ~5.3:1). Token-driven, so most screens reskinned at once; hand-edits to `Home.jsx` (relit landing + light `GuestMenu`), `WebShell.jsx` (aurora root, navy bar kept), `ui.jsx` (cached chip cooled), `Thumb.jsx` (warm tints cooled), `.skel`. Identity preserved (leaf, tagline, live-engine line, navy chrome, emerald, three doors, guest identity). Build 336 KB, **zero new deps**, no backend change. **Spine SL-001 regression-clean — byte-identical** (`local_p2p +₹83` = 125−40−2 vs `warehouse −₹129`, ₹212 swing, 597 km), confirmed via a full live walk (upload → live-gemini grade D/90%/same-unit → Recovery Path). 0 console errors; no `#f6f3ec`/cream tokens left. Frontend pushed → Vercel. Original spec retained below.

**Why this exists:** the build is feature-complete but the visual layer is a liability on stage. The canvas token is `--color-sl-paper: #f6f3ec` — a **warm cream/parchment**, which is the single most recognisable "an AI made this" tell of 2026 (the whole `--paper`/`--cream`/`--sand` warm-neutral band). The user wants the demo to read **whitish, clearly relatable to Amazon, but innovative — not a copy of Amazon's storefront.** A clean cool off-white canvas + the existing navy Amazon chrome + the Second Life emerald is exactly that: instantly "this lives inside Amazon," obviously *ours*, and it de-slops the look. **Presentation change, not a feature change** — same lesson as MT9 (de-phone). The ⭐ spine logic, every number, and the whole backend stay untouched.

**DECISIONS LOCKED (user, this session via /impeccable):**
1. **Whitish EVERYWHERE, including the landing** — the dark `stage-bg` landing goes light too (no dark-hero split). The user explicitly wants the landing whitish.
2. **Background flavor = emerald aurora + micro-grain** — a soft emerald light pooled in the top corners (~3–4% on white) + ~2% film grain overall. Subtle, alive, never fights content. (NOT glassmorphism, NOT gradient text, NOT a heavy texture.)
3. **Identity PRESERVED, not rebuilt** — keep the navy chrome, the emerald, the leaf wordmark, "Every product finds its next best owner," the live-engine line, the three-door landing concept, the guest identity. This reconciles the earlier saved preference ("keep the dark-stage landing, don't over-redesign, scope tightly") with today's whitish ask: it's a **reskin of the visual system, evolving the surface while keeping the brand spine.** The /impeccable redesign guidance agrees — when committed brand colors already exist in tokens, **identity-preservation wins**; audit-first, evolve in place.
4. **Zero new deps; CSS-only motion** (matches every prior MT). Add a motion lib ONLY if a specific effect genuinely needs it — default is the existing keyframes in `index.css`.

**IRON RULE UNAFFECTED:** the redesign is visual only — every on-screen number already traces to an endpoint (`/route`, `/metrics`, `/grade`, …). Do NOT touch `lib/api.js`, `App.jsx` state/logic, or any backend. If a number changes, something is wrong.

**Approach — EVOLVE IN PLACE (do NOT rebuild).** Most screens inherit color from the `@theme` tokens + shared chrome, so the bulk of the reskin is **retuning tokens in `index.css`** + rewriting the two surfaces that hardcode the dark look (`Home.jsx`, `WebShell.jsx`). Keep the `App.jsx` state machine, `lib/api.js`, the motion keyframes, and ALL backend logic.

### Scope (build order — lowest-blast-radius first)
1. **Tokens + ambient background (`frontend/src/index.css`)** — the lever that reskins most screens at once:
   - `--color-sl-paper: #f6f3ec` → a **true cool off-white** (~`#fbfcfd`, OKLCH near-white with a *whisper* of emerald tint, chroma ≈0.004–0.008 toward the brand hue — NOT toward warm). `--color-sl-line` (cream `#e7e3d8`) → a cooler hairline. Re-check `--color-sl-muted` against the new bg for **≥4.5:1 body contrast** (impeccable rule — the cream default may have been hiding low contrast; bump toward ink if close).
   - NEW light ambient layer to replace `.stage-bg` on the landing: emerald aurora = two low-opacity emerald radial-gradients in the top corners on the off-white (the light twin of the existing dark `.stage-bg`). NEW light micro-grain (reuse the `.stage-grain` SVG-noise trick at ~2% on light). Keep the dark `.stage-bg`/`.stage-grain` defined OR remove if nothing else uses them (grep first).
2. **Landing (`frontend/src/screens/Home.jsx`)** — currently a full-screen dark `stage-bg stage-grain` flex with white-on-dark text + dark `GuestMenu` + translucent-white `ViewButton`s. Re-lay on the light aurora canvas: navy/ink wordmark + display headline, the three doors as **clean white door-cards** (hairline + soft `--shadow-card`, the primary "Returns desk" still emerald), the live-engine line in muted ink. **Restyle `GuestMenu` for light** (it's `bg-white/[0.08]`/`text-white` today → invisible on white). Keep the same structure, copy, and props.
3. **Shell + chrome (`components/WebShell.jsx`, `components/TopBar.jsx`)** — KEEP the navy brand bar (that's the Amazon read; do not lighten it). Make sure the navy bar sits well over the new off-white column; `TopBar` is already a light page header (verify it reads on the new bg). The identity chip stays.
4. **Screens + components sweep** — walk every screen for cream-specific styling that now looks off on cool off-white: `Inbox` (incl. the `StageToggle` light pill — restyled light in MT9, re-verify), `ItemIntro`, `Grade`, `RouteScreen` (the MT12 destination-hero), `HealthCard`, `RadarScreen`, `LiquidityScreen`, `SealLane`, `DiagnoseScreen`, `MetricsScreen`, `BuyerStore`, `Checkout`, `Pdp`, `SellerDashboard`, `ResellConfirm`, `ResellPrice`, `FlashDeals`, `FlashDealDetail`, `MyResells`; components `Thumb`, `RadarToast`, `ui.jsx` primitives. Hardcoded `bg-[#...]`/cream/`bg-white/NN` values that assumed the old paper → move onto the new tokens. `skel` shimmer base (`#ece8de`, cream) → cooler. Keep the grade-color + money-semantic tokens as-is (they read on light).
5. **Polish pass** — hover/focus states, page-transition motion where it reads better on the lighter surface (respect `prefers-reduced-motion` — already wired), `text-wrap: balance` on the big headings, contrast spot-checks. Use the design skills (/impeccable polish, frontend-design, high-end-visual-design).

### Backend work
**NONE.** Frontend-only. No `deploy.ps1`, no new endpoints. Ships via `git push` → Vercel (like MT4).

### Watch-outs (grounded in lessons.md)
- **Contrast is the #1 risk** (impeccable): body text ≥4.5:1, large ≥3:1, placeholders too. The old cream may have masked a low-contrast `sl-muted`; verify on the new bg.
- `Home` renders **outside** `WebShell` (`App.jsx`: `if (screen==="home") return <Home/>`), so the landing has no navy bar — its background must be self-contained (the aurora layer lives on Home's root, like `stage-bg` did).
- When restyling a shared chrome component, grep its consumers (MT9 lesson: `TopBar` going light broke only `Inbox`'s `StageToggle`).
- `RadarToast` + error notes are `fixed inset-x-0 bottom-0 mx-auto max-w-md` — keep them readable on light.
- `Thumb` has an `onError` category-tile fallback — re-check the fallback tiles on the new bg.
- CORS: dev must run on **`http://localhost:5173`** (not 5174/127.0.0.1) for any API-touching screen (recurring lesson).

**Verify (MT13 done when):** every screen + the landing render on the new whitish canvas with the emerald-aurora + micro-grain, walked via chrome-devtools on localhost AND the live Vercel URL; **identity preserved** (leaf wordmark, tagline, navy chrome, emerald, three doors, guest identity all present); **body contrast ≥4.5:1** spot-checked on representative screens; **spine (SL-001) regression-clean** — the flow is visually new but `local_p2p +₹83` vs `warehouse −₹129` and every number are byte-identical (presentation only); no dead buttons; `prefers-reduced-motion` honored; **build clean, zero new deps**; no `#f6f3ec`/cream tokens left; fresh-context verifier (or /impeccable audit) signs off; frontend pushed → Vercel serving the new look. Update STATE.md + lessons.md.

**SEQUENCING:** Build MT13 **before MT5** so the bulletproof pass + loading/error-state audit cover the *new* UI, not the old one. Then MT5 (bulletproof) → MT6 (PRD + video + submit). MT13 does not touch the locked spine, the backend, or any number.

## MT14 — Trust & the closing loop (A backend + B frontend) ✅ DONE (this session, redeployed digest `sha256:0cea229f…`)
**Result:** all 3 build items + 4 docs-only roadmap entries shipped. Spine regression-clean on the live Function URL (`local_p2p +₹83` vs `warehouse −₹129`); build clean (339 KB, **zero new deps**); backend redeployed via `deploy.ps1` and every touched route re-curled; frontend pushed.
- **fix-1 (frontend-only):** `FlashDealDetail` reports the `interest_id` it created → `App.trackInterest` → an `App` poller watches `/resell/listings`; when the reseller accepts that interest (status `accepted`, listing `sold`) the buyer gets an "Order confirmed 🎉" popup + a sticky notification (`confirmedOrders` → `BuyerStore` Notifications, `· N` badge). Declined/passed buyers → nothing. No backend change (accepted-interest status was already in the listing payload).
- **fix-4 (server-side):** diagnosed the user-reported break — the separate `POST /resell/from-route` (frontend, `origin==="ops"`) could hit a different warm Lambda with no `ROUTED` event → silent no-op. `vrs.route_item` now lists the graded local_p2p return **inside `/route`** (atomic with `ROUTED`, idempotent, best-effort, local import); dropped the fragile frontend POST. Verified live: `/resell/listings` shows SL-001 (D ₹132) + SL-006 (B ₹2177), `source:"return"`.
- **fix-2+3:** `vrs.build_paths` gates `dark_store` by category (`RENEWED_CATEGORIES={"electronics"}`); electronics' eligible local_p2p carries `renewed_channel` (no dark_store) + `/route` has a top-level `quick_commerce_eligible`. No economics change. `RouteScreen` shows quick-commerce ("Moved to quick commerce · Amazon Now · {MFC}") vs "♻️ Amazon Renewed · certified"; a seeded `usage_cert` battery line on SL-002/SL-006 surfaces on the Health Card as a CERTIFIED row. Added the SonicWave headphones (B0HDPHN880→SL-006) to Rahul's orders (window open 7d) + `cached/SL-006.grade.json` (grade B) so Return→Ops→grade→route→Renewed (+₹1,987) is demoable. Also finished the half-done "every order is resellable" commit (`orders.py` resellable = has item_id; the App `startResell` gate was dead for non-monitor orders). VRS test extended (Renewed-vs-quick-commerce assert) — passes.
- **Docs:** api-spec (`/route` new fields + server-side listing, `/health-card` usage_cert), architecture §13, PRD §7 roadmap. **In-browser walkthrough left to the user** (their instruction). ⚠️ Per-instance store caveat unchanged (listings/interests/confirmed-orders reset on cold start; the two-person resell+confirm beat wants one warm instance + two identities). Original scope retained below for reference.

**Why this exists:** a batch of playbook/strategic-review items + current-implementation fixes, split (with MT15) into two sessions. MT14 is the **trust half**: prove the grade is trustworthy and close the buyer-facing loop. Full design + verification-of-existing in `docs/superpowers/specs/2026-06-14-mt14-mt15-backlog-design.md` (read it first). **Iron rules hold:** protect the SL-001 spine (`local_p2p +₹83` vs `warehouse −₹129`); every on-screen number API-backed; redeploy + curl-verify (never trust "Done. Deployed" — lessons.md); zero new deps.

### Scope — build
1. **🟡 Resell sold → buyer "Order confirmed" (fix-1).** When the reseller accepts an interested buyer, the *buyer* side gets an "Order confirmed" notification + a small popup. `resell.sell_to_interest` (`resell.py:190`) already flips the listing to sold + records the accepted interest — surface that accepted-interest status to the buyer's view (the interested party), and fire a popup/toast on the buyer side. No new economics. *Verify:* reseller accepts on My-resells → the buyer (the interested tab/persona) sees "Order confirmed" (notification + popup); declined buyers don't.
2. **🟠 Returns → Flash deals fix (fix-4) — user-reported broken.** Today the board listing only fires from the frontend on `origin==="ops" && r.decision==="local_p2p"` via a separate POST (`App.jsx:246` → `api.listFromRoute` → `POST /resell/from-route/{id}`), which can miss (per-instance store, or the graded return doesn't win local_p2p). **Diagnose with systematic-debugging**, then make `/route` create the board listing **server-side** when the winner is `local_p2p` and the route originated from a return (atomic with the ROUTED event, same instance), reusing `resell.list_from_route` (`resell.py:134`). Keep the frontend POST as a harmless no-op or drop it. *Verify:* grade a buyer-returned item on the Ops desk → it routes local_p2p → it **shows on Flash deals** with the "Returned · Grade X" badge (`FlashDeals.jsx:63`), surviving a cold start / cross-tab; resells still show; spine clean.
3. **🟠 Electronics order + Renewed routing (fix-2 + fix-3).** Add an electronics item to `seed/orders.json → rahul_order_history`. On resell/return it routes to **Amazon Renewed (certified)**, NOT Amazon Now quick-commerce. Thin backend in `vrs.build_paths` (`vrs.py:105-110`): gate the `dark_store` (quick-commerce) attachment by category, add a `quick_commerce_eligible` bool to the route response + a seed-backed `renewed_channel` descriptor for electronics; seed a **battery-cycle line on the Health Card** as the trust hook (the deep usage-cert telemetry stays roadmap — see below). Frontend `RouteScreen.jsx`: non-electronics local_p2p shows **"Moved to quick commerce · Amazon Now · {dark store} · {km}"**; an item that is **not** quick-commerce eligible shows its **best VRS path** instead (electronics → "Amazon Renewed (certified)"). Every figure still traces to `/route`. *Verify:* the electronics order routes to Renewed (not quick-commerce) with the battery-cycle trust line; a non-electronics local_p2p shows "Moved to quick commerce"; the not-eligible card names the best path; spine (SL-001, footwear) unchanged.

### Scope — docs-only roadmap (PRD §7 + architecture.md, no code)
- **#1 Buyer-verified closing loop §3.6** — buyer of a second-life item taps "Yes, condition matches Grade X" → ~₹15 credit; each tap is a free human label auditing the AI grade ("the only grading model audited by a human on every sale"). Strong; deferred to roadmap this pass.
- **#10 Review-informed inspection checklists §18.2** — mine a product's own customer reviews to auto-build a per-SKU inspection checklist (one extra LLM call).
- **#2 Agent-as-grader / Agent Flip §3.3 §4.3** — delivery agent grades at the doorstep during pickup; Agent Flip posts the graded item to that route's flash deals.
- **#12 Usage-data certification §5.3** — Health Card shows quantified device usage (battery cycle count, sensors verified) for electronics — the deep version of the battery-cycle hook built above.

### Backend / frontend / verify
- Backend: `resell.py` (surface accepted interest to the buyer; server-side `list_from_route` from `/route`), `vrs.py` (`quick_commerce_eligible` + `renewed_channel` by category), `seed/orders.json` (+ electronics order), Health Card seed (battery-cycle line). Register/adjust in `main.py`; **redeploy via `deploy.ps1`** + curl each touched route. Document new fields in `docs/api-spec.md` + `architecture.md`.
- Frontend (reuse design system, zero new deps): buyer "Order confirmed" popup/notification; `FlashDeals` unchanged (server-side listing feeds it); `RouteScreen` quick-commerce vs Renewed/best-path card.
- **Verify (MT14 done when):** all three build items pass their checks above; spine (SL-001) regression-clean; build clean, zero new deps; backend redeployed + re-curled; PRD §7 + architecture carry the 4 roadmap entries; walked via chrome-devtools on localhost AND the live Vercel URL; STATE.md + lessons.md updated.

## MT15 — Make the invisible warehouse visible (A backend + B frontend) — ✅ DONE (June 14, deployed + curl-verified live)
**STATUS:** both build items shipped + redeployed to Lambda + verified on the Function URL; 7 roadmap entries added to PRD §7 + architecture §14; api-spec.md documents both endpoints. Spine regression-clean (`tests/test_vrs.py` passes, SL-001 `+₹83 / −₹129`); frontend builds clean (344 KB, zero new deps); frontend field names cross-checked against the live API shape. In-browser walkthrough on localhost + Vercel left to the user (their instruction). See STATE.md Done block.
**Why this exists:** the **activation half** of the split — surface and activate idle inventory in people's homes, from the owner's side (the pull twin of the Idle-Asset Radar's push). Design in `docs/superpowers/specs/2026-06-14-mt14-mt15-backlog-design.md`. Same iron rules as MT14.

### Scope — build
1. **🟠 "Your Things" dormant-value dashboard (#9 §18.3).** NEW `GET /your-things/{persona}` → every order in the persona's history with a live resale value (`pricing.resale_value` + age + the built life-stage curve from `lifestage.py`) and a running total ("your home holds ₹X in dormant value"). NEW `YourThings` buyer screen → list + big total + per-item **Resell** CTA into the existing resell flow (`startResell`). *This was cut in MT4 (Open items) only because no endpoint backed the numbers — now one does, so no hardcoded JSX.* *Verify:* every number on Your Things traces to `/your-things/{persona}` (network tab clean); the total = sum of the rows; Resell opens the existing flow.
2. **🟡 Personal Green Ledger (#6 §8.5).** NEW `GET /green-ledger/{persona}` → that persona's own CO₂ / landfill-kg / items-diverted, derived from their passport events (reuse `metrics.py` math, scoped per persona). Shown **subtly on BOTH the buyer side (a strip on Your Things) and the seller dashboard** — a small ledger line, not a hero panel. *Verify:* the buyer ledger and the seller ledger both trace to `/green-ledger/{persona}`; subtle placement; spine clean.

### Scope — docs-only roadmap (PRD §7 + architecture.md, no code)
- **#3 Hidden QR resale + gift transfer §2 §18** — every product ships a QR; scanning opens a prefilled resale page (Amazon already knows the item); a gift recipient scans → the passport ownership transfers to them so they can resell an item never in their order history. *(The school-textbook + life-stage "you probably don't need this" nudge is the cheapest slice to pull forward later — life-stage is already built — but deferred per the user.)*
- **#4 Brand layer §18.5** — brands get right-of-first-refusal on their own returns (buy back graded units before public relist) + fund trade-in boosts; surfaced on the seller dashboard. Re-tagged roadmap (no build) this pass.
- **#8 Pickup piggybacking §8.4** — an agent already driving to an address is shown nearby dormant items to collect on the same trip (~₹0 marginal cost to harvest the invisible warehouse).
- **#9 Season-aware routing §8.6** — demand timing becomes a VRS input (a winter jacket holds or routes to a still-cold region; festival spikes raise local resale price) — one line in the VRS math.
- **#14 Material-stream routing §18.7** — genuinely-dead items route by material (cotton → textile recyclers, electronics → e-waste) → India's EPR law → sellable compliance certificates; the per-material mechanic behind the EPR revenue line.
- **Grow Cycle subscription §7.2** — a swap subscription for outgrowable categories (baby gear, kids' shoes): outgrow → agent collects + relists the old, delivers the next size up.
- **Micro-liquidation bidding §4.7** — local kiranas / neighbourhood resellers bid on individual items in their pin code (40%+ recovery) instead of bulk-pallet liquidation (10–15%) — the lowest tier of the existing VRS cascade, as a local market.

### Backend / frontend / verify
- Backend: NEW `GET /your-things/{persona}` + `GET /green-ledger/{persona}` (reuse `pricing.py`, `lifestage.py`, `metrics.py`); register in `main.py`; **redeploy via `deploy.ps1`** + curl both. Document in `docs/api-spec.md` + `architecture.md`.
- Frontend (reuse design system, zero new deps): NEW `YourThings` screen + per-item Resell CTA; subtle green-ledger strip on Your Things AND on `SellerDashboard.jsx`.
- **Verify (MT15 done when):** both build items pass; spine (SL-001) regression-clean; build clean, zero new deps; backend redeployed + re-curled; PRD §7 + architecture carry the 7 roadmap entries; walked via chrome-devtools on localhost AND the live Vercel URL; STATE.md + lessons.md updated.

**SEQUENCING:** Run **MT14 → MT15 → MT5 → MT6**. Both land before MT5 so the bulletproof + loading/error-state pass covers the new surfaces (buyer-confirm popup, server-side return listing, electronics Renewed card, Your Things, per-persona green ledger). Neither touches the locked spine.

---
**Reserve the final 6–8 hours exclusively for MT6.** | Done log: see docs/STATE.md
