# Project State — Amazon Second Life

## Status: GRADING HARDENED (pre-MT5) + redeployed + verified live (June 14, 2026) — fixed the grader the user flagged (identical photo → B; random image still graded). Root cause was demo data + prompt + a missing gate, not plumbing: SL-001's seed baseline (North Star) vs seeded current (Nike) were different shoes; the prompt punished lighting/angle as "defects"; nothing gated a non-matching unit. Changes: **switched the grader to Gemini 2.5 Flash primary / Nova 2 Lite failover** (Gemini Pro free-tier is quota-walled — unusable as a live primary; Flash verified working), **delta-honest prompt** (ignore lighting/angle/crop/compression — only real wear counts), **same-unit trust gate** (`needs_human_review`+`review_reason` when not same unit / low confidence; non-blocking), and **Gemini JSON-mode + thinking disabled** (Flash's reasoning tokens were truncating the JSON). Re-captured SL-001/SL-002 caches. Verified live on the Function URL: upload-catalog-as-current → A/A/A; Nike mismatch → D/D/D flagged; all live-gemini, 3/3 consistent; spine `/route` SL-001 still `local_p2p +₹83`. Backend redeployed (digest confirmed), frontend pushed. **MT5 is next.**

## Status (prior): MT10.1 done + redeployed + verified in-browser (June 14, 2026) — fixed the 3 broken MT10 flows the user reported: (1) resell now has accept/decline + one-tap sell-to-buyer (listing→SOLD, ₹net credited); (2) buyer returns appear on the Ops desk in real time AND are clickable into the grading spine (added SL-003 grade cache); (3) the notification "Resell now" uses the SAME resell path as order history (old radar resellOrder deleted). Walked end-to-end via chrome-devtools on localhost against the deployed backend — 0 console errors. Backend redeployed; frontend pushed.

## Status (prior): MT10 done + redeployed + pushed (June 13, 2026) — buyer-flow fixes (return window, de-mixed Ops, personal size) + full resell redesign (confirm→photo→AI price→price/range→flash-deals board→live cross-tab interest). Backend redeploy unblocked permanently via ECR cred-helper.

## Done
- ✅ PS + angle locked: Stores "Products Without a Second Chance" → Second Life (delta-grading + VRS interception + Health Card + Idle Asset Radar spine), per STEP 1 + strategic review of the master playbook
- ✅ All plan files written: CLAUDE.md (project + environment), docs/PRD.md, architecture.md, api-spec.md, tasks.md, demo-and-prfaq.md, db-setup.md, lessons.md
- ✅ Boilerplate deployed & verified (pre-hackathon): FastAPI on Lambda (ca-central-1, Function URL) + Bedrock Nova 2 Lite w/ Gemini failover + React/Tailwind on Vercel
- ✅ **MT1 — Seed store + Product Passport + Delta-Grader** (commit `be97d15`). Local-verified, fresh verifier signed off. Built: `seed/` (8 items + orders + neighbors JSON, placeholder images for SL-001 shoe & SL-002 monitor, cached grades), `passport.py` (in-memory event log + DynamoDB write-through behind flag), `grading.py` (Nova-2 multimodal delta-grader, Pydantic schema, retry-on-bad-JSON, Bedrock→Gemini→cache), `llm.py` `ask_llm_images()`, endpoints `GET /items`, `GET /items/{id}`, `POST /grade`, `scripts/capture_cache.py`. Hero shoe grades **D/D/D** across 3 live runs (consistency ✅); creds-broken server returns identical shape with `source:cached` ✅.
- ✅ **MT2 — VRS engine + Health Card + RTO + Radar + Pricing + deploy** (this session). **DEPLOY BLOCKER RESOLVED** — `deploy.ps1` now pushes to ECR and updates Lambda successfully (IAM fixed). All endpoints in docs/api-spec.md are LIVE on the Function URL and a fresh verifier signed off (9/9). Built: `pricing.py` (per-category depreciation, grade factors, resale, −5%/wk price decay, liquidity curve), `vrs.py` (6-path engine, breakdowns that sum exactly to recovery, eligibility gates, winner=argmax, co2/km saved; sealed RTO skips grading→grade A), `radar.py` (Idle Asset Radar over orders/neighbors), `healthcard.py` (provenance + warranty calc + price decay), `inspection.py` (`/seal-check` + `/diagnose-listing`, live-AI→cache), `metrics.py` (passport + baseline counters). New endpoints: `POST /route`, `GET /health-card/{id}`, `POST /seal-check`, `GET /radar/{asin}`, `GET /price-curve/{id}`, `POST /diagnose-listing`, `GET /metrics`. Cached AI responses added: `SL-004.seal.json` (SEALED_NEW), `SL-003.diagnose.json` (navy→royal blue). VRS constants LOCKED in architecture.md §4. Hero shoe → `local_p2p` winner at every grade (grade D: local **+₹83** vs warehouse **−₹129**); sealed mixer → `rto_relist` **+₹2,464**.

- ✅ **MT3 — Frontend spine: the demo console** (commit `722a605`). The 5-screen hero flow is clickable end-to-end on the LIVE Vercel URL (**https://amazon-hackon.vercel.app**) against the deployed backend, verified via playwright + chrome-devtools on BOTH localhost and production. Built (`frontend/src/`): `lib/api.js` (every endpoint + `routeSafe`/`healthCardSafe` that re-run prereqs on a 409 cold-start so the per-instance passport never breaks the spine; deployed Function URL baked as fallback so Vercel works without a dashboard env), `lib/format.js` (INR + count-up), `components/` (PhoneFrame brand-rail + device shell, TopBar, Thumb with category-tile fallback for missing photos, RadarToast, ui primitives), `screens/` (Inbox, ItemIntro guided-capture, Grade, RouteScreen VRS, HealthCard). Design: Amazon-navy chrome + Second Life emerald identity, Bricolage Grotesque/Plus Jakarta Sans, CSS-only motion, **zero new deps**. Hero SL-001/SL-002 real photos copied to `frontend/public/items/`. Verified: live Bedrock grade (D, 90–95%) AND instant CACHED path (force_cached toggle in the inbox) — visually identical; VRS winner local_p2p **+₹83**, breakdown reconciles (125−40−2), warehouse **−₹129**, "₹212 swing"; Health Card warranty-transfer + price-decay sparkline; radar ping toast ("3 matched buyers within 4 km"). **0 console errors** (only 6 benign image-404s for the photoless filler items → Thumb fallback). **DynamoDB write-through CONFIRMED** — GRADED/ROUTED events persist to `SecondLifePassport` (db-setup.md step 4 ✅).

- ✅ **MT4 — Frontend moments + supporting screens** (commit `4f71166`). Every remaining demo beat is now reachable off the inbox on the LIVE Vercel URL against the deployed backend; verified via chrome-devtools on localhost AND production. The inbox now wires four lanes (`App.jsx` branches by `item_id`); SL-002/003/004 were display-only "QUEUED", now tappable; SL-005..008 stay QUEUED. New screens (`frontend/src/screens/`): **RadarScreen** (SL-002 idle → `/radar/{asin}`: live-demand banner "3 buyers searching 'baby monitor'", an SVG radar plotting 12 dormant units by real distance with Rahul's unit pinged, total dormant value ₹20,600, owner list) → **LiquidityScreen** (`/price-curve/SL-002`: reactive price↔speed slider + tappable demand-curve bars 4/4/3/2/1, engine recommendation ₹1,750/2-buyers/~4d, "BEST" badge) → ping toast w/ escrow line; **SealLane** (SL-004 RTO → `/seal-check`: SEALED_NEW + 96% confidence, "skips grading → grade A") → reuses **RouteScreen** (rto_relist wins **+₹2,464**, breakdown reconciles, warehouse +₹1,810 "₹654 swing") → "List for local pickup" → ping; **DiagnoseScreen** (SL-003 → `/diagnose-listing`: 18 returns, navy→royal-blue swatches, before/after auto-patch, −40% count-up, apply-fix flips inline); **MetricsScreen** (banner → `/metrics`: recovered vs write-off, warehouse-bypass ring, green ledger CO₂/landfill/inspection, take-rate line). `api.js` added `routeRto` (seal→route on 409) + `priceCurveSafe` (cached grade on 409). `RadarToast` generalized to `{title,message}`; `RouteScreen` got optional `nextLabel`/`nextHint`; frosted `backdrop-blur-sm` on the sticky footer; `sl-range` slider CSS. **Zero new deps**, build clean (263KB JS). Console: only the 6 documented benign image-404s + 1 expected/handled 409 (priceCurveSafe cold-start retry). Spine (SL-001) regression-clean end-to-end. ⚠️ NOTE: `/metrics` is cumulative over the warm Lambda instance (baseline 5 + every GRADED/ROUTED since cold start), so the "this session" counter drifts up across demo runs — prod read ₹3,203/83% after my test routes vs the ₹3,120/80% baseline. Confirms numbers are live, but for a clean stage number consider a cold instance or a reset (MT5).

- ✅ **MT9 — Web console redesign: de-phone the demo** (this session). The whole demo moved off the 390px phone frame into a real **web console**, verified end-to-end via chrome-devtools on localhost against the redeployed backend (prod verify after push). Built: `WebShell` (persistent Amazon-navy brand bar + search + bell + cart-count over a centered `max-w-6xl` column) replaces `PhoneFrame` (deleted); `TopBar` → light page header (dropped the iOS status row); `.screen-scroll` → centered reading column + `.screen-page` for wide screens; `Home` → web landing grid; `ItemIntro` → **two-pane scan station** (LEFT day-0 baseline · RIGHT upload dropzone → live grade of UPLOADED photos); new `BuyerStore` (Shop/Cart/Orders/Notifications) + `Checkout` (UPI) replace `BuyerHome`; `SellerDashboard` → wide return-rate table; `lib/image.js` (canvas downscale to ~1024px). Backend: `/grade` gained `current_images` (base64, ≤3, ≤1.5 MB ea., 422 on oversize/invalid; `graded_uploaded_photos` in response); new `buyer.py` + `seed/buyer.json` → `GET/POST /cart/{persona}`, `GET /notifications/{persona}`, `POST /checkout/{persona}` (demo UPI pending→success, cart empties). **Redeployed via deploy.ps1** (digest pushed, Lambda updated). Zero new deps (build 300KB JS). **Verified LIVE on localhost:** upload SL-001 current photo → `/grade` body carried `current_images` → live-bedrock grade D/90% of the uploaded photo (`graded_uploaded_photos:true`) → route local_p2p +₹83 (125−40−2) → Health Card (₹132, warranty TRANSFERS, decay) → radar ping; Shop→PDP(68% fit proof, UK 9 PICK, resale ₹343)→add to cart(server-merged qty 2, ₹1,899)→UPI pending→success(order+amount preserved across confirm); Orders→resell monitor→radar; Notifications nudge→radar; Seller table(kurta 28%)→diagnose(navy→royal, Apply→−40%). **0 console errors.** Fixed: checkout confirm now preserves the approved order id + amount (cart is per-instance, a confirm on another warm Lambda could recompute a different total).

## Just shipped (landing guest identity, June 14 — frontend-only)
- **Guest login on the landing → unique identity per person.** Top-right "Sign in as guest" pill on `Home.jsx`: pick a role (Buyer · Seller · Returns desk) → mints a unique per-browser id (`Guest-XXXX`, persisted in `localStorage.sl_guest`); login sets identity, the three big buttons still enter (and tag the role). `App.jsx` split the single hardcoded `PERSONA="rahul"` into **`SAMPLE="rahul"`** (shared sample storefront data — cart/orders/notifications/size-advice, the user's "shared sample data" choice) and **`me = guest?.id || "rahul"`** used only where ownership matters: resell `createListing` owner, `FlashDeals` own-listing check, `MyResells` filter, and return attribution. `WebShell` shows the identity chip in the navy bar so the demo reads clearly. **Why it matters:** with one identity every demo person collapsed into "rahul" so My-resells ≡ Flash-deals and returns weren't attributable; distinct guest ids make the resell marketplace + Ops funnel legible. **Two-person resell demo now needs two identities** (two browsers / incognito / the "+ New guest identity" reset) — same browser+localStorage shares the id. Verified in-browser (chrome-devtools): menu → pick Buyer → chip `Guest-5MG5 · Buyer` → enters the storefront with the chip carried into WebShell. Build clean (326 KB, zero new deps). Frontend-only — no backend change (owner/persona is already a free string). ⚠️ Buyer header still reads "Hello, Rahul" (shared sample storefront) while the chip shows the guest — intentional (you browse the shared sample as your guest identity).

## Just shipped this session (commits 6ea6749 + ddce925)
- **MT9 landing fix (6ea6749):** the MT9 web-grid landing lost the original identity the user liked. Restored the original dark `stage-bg`/`stage-grain` brand-rail landing ("Every product finds its next best owner" + the description + the live-engine line, original colors). The phone is replaced by **three forwarding buttons** — Returns desk (Ops) · Buyer · Seller — each *label + one-line role*, stacked where the phone sat. **No top bar on the landing** (`App.jsx` renders `Home` full-screen *outside* `WebShell`). Slimmed the inner `WebShell` chrome to just leaf + "Second Life" (dropped search box, bell, cart count, and the "a layer inside Amazon" tagline); the per-screen `TopBar` still carries back + page title beneath. Buyer cart is still reachable via the in-page Cart tab. Frontend-only; pushed → Vercel.
- **MT8 (ddce925) — Dark-store node + derived value cascade.** Backend redeployed via deploy.ps1; `/route` `dark_store` + `/cascade/{id}` verified on the deployed Function URL. See Done below.

## Done (this session additions)
- ✅ **MT8 — Dark-store node + derived value cascade** (commit `ddce925`, redeployed). **Move 1:** NEW `seed/dark_stores.json` (5 Amazon Now MFCs, Bangalore lat/lon) + `seed.nearest_dark_store()` (haversine) → `vrs.build_paths` attaches `dark_store {id,name,distance_km}` to an eligible `local_p2p` path; web `RouteScreen` renders "Lists open-box at **Amazon Now · Indiranagar** · 1.9 km" on the winner card. **Move 2:** NEW `cascade.py` + `GET /cascade/{item_id}` re-runs the VRS argmax week-by-week under −5%/wk decay, emits a tier each time the winning channel flips, terminates at `donate` (CSR). **Pure-Python deterministic — no AI, no cache, can't fail live.** Refactored `vrs.route_item` into a shared, side-effect-free `build_paths()` + `grade_for()` so route and cascade share ONE path builder — **spine math unchanged** (local_p2p still **+₹83** = 125−40−2; all eligible paths still reconcile). Frontend: `CascadeStrip` below the path list, `api.cascade`/`cascadeSafe` (cold-start re-grade), fetched **non-blocking** after route (RouteScreen never waits on it); RTO lane passes `cascade={null}` (sealed units have no GRADED event). **Verified on the deployed Function URL:** `/cascade/SL-001` 409 without grade; after grade → `local_p2p +₹83 (week 0)` → `donate +₹45 (week 8, terminal)`, decay 5; `/route` local_p2p carries `dark_store DS-09 Indiranagar 1.9 km`. **Cascade depth is an honest function of the LOCKED constants:** SL-001 (₹500 shoe) falls local→donate (2 tiers); higher-value/repairable items go deeper (SL-006 headphones grade C: refurbish→donate). Move 3 (cross-dock animation) NOT built (STRETCH, cut from the bottom). Zero new deps (build 299KB JS). Docs (api-spec.md) updated to the real SL-001 output. ⚠️ The cascade renders only on the **spine** RouteScreen in the demo (SL-001); SL-002/SL-006 cascades are richer but those items don't reach RouteScreen in the UI (radar/QUEUED lanes).

## Done (this session additions)
- ✅ **MT10 — Buyer-flow fixes + resell redesign** (commits aab8570..2d1e1fa). Five fixes, one MT. **Backend (redeployed):** `orders.py` return-window fields + 2 recent Rahul orders; NEW `returns.py` + `seed/returns_seed.json` + `GET/POST /returns`; `size.py` + `seed/purchase_profile.json` personal block + `/size-advice/{asin}?persona=`; NEW `resell.py` + `/resell/quote|listings|interest`. **Frontend (pushed → Vercel):** `BuyerStore` return-window gate + Flash deals/My resells tabs; `Inbox` two-section returns desk reading `/returns`; `Pdp` personal card; NEW `ResellConfirm`/`ResellPrice`/`FlashDeals`/`MyResells`; `App` wiring. Spec + plan in docs/superpowers/. **Verified on the deployed Function URL** (every new endpoint curled; spine SL-001 grade D clean; net peaks mid-range 935→1009→961 @3/7/15km). Build 317 KB, zero new deps. **Deploy blocker fixed permanently** — ECR push routed through the `ecr-login` cred helper (see lessons.md). ⚠️ Returns/listings/interests are per-instance (cart pattern). **Browser walkthrough delegated to the user** (their instruction). See docs/tasks.md MT10.

## In Progress
- (Nothing mid-task. MT10 + MT10.1 closed; **grading-correctness hardening done + deployed + verified** (June 14). **MT5 is next** — bulletproof + polish + repo, now incl. MT7+MT9+MT8+MT10/.1, and the grading hardening folds into MT5's failover-drill + cached-response audit.)

## Done (this session — grading hardening, pre-MT5)
- ✅ **Grading correctness hardening** — user-flagged: identical-photo→B and random-image-still-graded. **Diagnosed** (root cause = demo data + defect-biased prompt + no identity gate, not the pipeline) and **fixed**: (1) **Gemini 2.5 Flash primary, Nova 2 Lite failover** — verified Gemini 2.5 **Pro** free-tier is quota-walled (429 RESOURCE_EXHAUSTED, unusable live), Flash works and has headroom; env flipped on Lambda + `.env`. (2) **Delta-honest prompt** in `grading.py` — rubric grades physical-condition change; prompt ignores lighting/angle/distance/background/shadows/image-quality. (3) **Same-unit trust gate** — `grade_item` now sets `needs_human_review`+`review_reason` when `same_unit.verified` false / `same_unit.confidence<0.50` / `confidence<0.70` (non-blocking; spine still routes; `Grade.jsx` shows the reason). (4) **Gemini reliability** — `response_mime_type=application/json` + `thinking_budget=0` (Flash's thinking tokens were truncating the JSON) + 18s timeout so a hung primary fails over in budget. Re-captured SL-001/SL-002 grade caches. **Verified live on the Function URL:** `/grade` live-gemini, gate fields present, cached mirrors live; spine `/route` SL-001 still `local_p2p +₹83`; 3/3 consistency (match A/A/A, mismatch D/D/D). Backend redeployed, frontend pushed. ⚠️ SL-002 monitor now grades **A** (its seed photos genuinely match) → its radar/resell recovery shifts up; SL-001 stays D so the hero math is unchanged. ⚠️ SL-001 demo still shows same_unit=false until the **user supplies real same-shoe before/after photos** (their chosen action) — pipeline is correct and waits on the photos.

## Done (this session — MT10.1)
- ✅ **MT10.1 — Resell sell-to-buyer + real-time inspectable returns + one resell path** (June 14). Fixes the 3 defects the user reported on MT10, verified in-browser. **Backend (redeployed):** `resell.py` listings gained `status`/`sold_to`/`net_earned` + `sell_to_interest`/`decline_interest`; `main.py` `POST /resell/listings/{id}/sell|/decline`; `returns.py` return entries now carry `item_id`; NEW `seed/cached/SL-003.grade.json` (kurta grade B). **Frontend (pushed):** `MyResells` rewritten with per-buyer Sell/Decline + take-home + SOLD summary (poll-paused during actions); `FlashDeals` hides interest on own listings + shows SOLD; `Inbox` `GradeableReturn` row ("Inspect & grade") for returns with an `item_id`; `App` adds `localReturns` merge (instance-proof real-time), `openReturnForGrading` (forces spine lane), unified `openNotif`→`startResell`, deleted orphaned `resellOrder`; `api.js` `sellToInterest`/`declineInterest`. **Verified live (chrome-devtools, localhost→deployed backend, 0 console errors):** notification Resell now → ResellConfirm (paid ₹3,200, est ~₹978) → price ₹1,750 → list → My resells; neighbour interest → Sell to Asha (declined Kabir) → SOLD + ₹1,683 credited; return kurta → appears instantly on Ops desk as "Inspect & grade" → grade B (cached) → VRS local_p2p +₹472 (522−40−10). Build 322 KB, zero new deps. ⚠️ Per-instance stores unchanged (returns/listings) — cold start resets to seed; keep one warm instance for the cross-tab beat.

## Built — pending formal close in Done (commit de98e75): **MT7 — Two-sided console: Prevent + Recirculate** (A backend + B frontend) — see docs/tasks.md MT7
Widens the demo from recovery-only to the full lifecycle **Prevent → Recover → Recirculate** (decided in the June 13 brainstorm). Adds a persona switch (Buyer · Seller · Ops) to the phone frame:
- **Buyer hub** — browse/PDP with **size social proof** ("68% of size 9 buyers went one up" = PREVENT) + **order history → one-tap resell** of the monitor, flowing into the EXISTING RadarScreen/LiquidityScreen (= RECIRCULATE, the Rahul beat with a real origin).
- **Seller dashboard** — per-SKU **return-rate list** sorted worst-first → tap a high-return SKU → REUSE `/diagnose-listing` for the "why + AI fix + −40% projected drop" (= PREVENT).
- **Recover** = the existing spine, untouched.
**Iron rule held:** every new number is API-backed via thin **seed-JSON + one-line endpoint** (mock the data, not the architecture) — NOT hardcoded JSX. New backend: `GET /size-advice/{asin}` (`size_signals.json`), `GET /seller/returns` (`seller_catalog.json`), `GET /orders/{persona}` (expose existing `orders.json`). **Backend redeploy required** (`deploy.ps1`, Docker up) — NOT frontend-only like MT4. Frontend reuses the MT3 design system, zero new deps. Full scope + verify checks in docs/tasks.md MT7.

## Then: **MT5 — Bulletproof + polish + repo** (A+B) — see docs/tasks.md
The demo cannot fail on stage. Scope: cached responses verified for all items × all AI endpoints (**incl. the MT7 + MT9 + MT10 screens**); kill-switch env (`FORCE_CACHED=1`); failover drill (break Bedrock creds → Gemini → cache); loading/error states audit (incl. MT9 upload + checkout + MT10 resell upload/quote + flash-deals/my-resells polling); web-console responsive polish pass; README with architecture diagram + live URLs; repo public; final deploy both ends. **MT10 NOTE for MT5:** returns/listings/interests are per-instance in-memory (cart pattern) — same cold-start caveat; the cross-tab resell demo needs both tabs on one warm instance. Real placeholder images for the 3 seeded return rows (RTN-2001..2003) still pending (Thumb tiles render meanwhile). **MT9 NOTE for MT5:** the buyer cart + checkout amount are per-Lambda-instance (cold start resets to seed) — same class as the passport; the frontend already pins the approved order across confirm, but consider a reset/cold-instance for a clean stage number (also affects the cumulative `/metrics`).
Verify: full demo run on live URLs with Bedrock disabled → no visible difference; fresh-context verifier subagent walks the script and signs off; Lighthouse sanity on the Vercel app.

## Open items (not blocking MT5)
- **MT4 scope cut from the bottom (per the "cut from the bottom" rule):** "Your Things" dormant-value dashboard, the standalone keep-it decision screen, and the mock PDP "options near you" row were NOT built — they need numbers no current endpoint returns (would mean hardcoded JSX, which the rules forbid) and aren't in the 3-min beat sheet. The escrow/locker-credit beat is folded into the ping-toast copy rather than a standalone screen. **UPDATE (June 13): the mock PDP idea is revived under MT7 as the buyer PDP with size social proof — but done right, backed by a new `/size-advice` endpoint (seed JSON), not hardcoded.**
- **Real demo photos** still pending (you'll add later). Today: SL-001/SL-002 ship real photos (in `backend/app/seed/images/` AND `frontend/public/items/`); SL-003..008 have NO photos → the frontend `Thumb` component renders clean category-tile fallbacks (no broken images; the 6 console 404s are expected/handled). When photos land, drop them into `frontend/public/items/SL-00X/{day0_1,day0_2,current_1}.jpg` (frontend-static) AND `backend/app/seed/images/SL-00X/` then re-capture caches. `/grade` for SL-003..008 still 502s live (cached covers SL-001/002); `/seal-check`(SL-004) + `/diagnose-listing`(SL-003) serve hand-authored cached responses.
- **CORS origin gotcha:** backend `ALLOWED_ORIGINS` = `http://localhost:5173, https://amazon-hackon.vercel.app`. Browser must use **`localhost:5173`** for local dev — `127.0.0.1:5173` is NOT allowlisted and gets "Failed to fetch". Add any new frontend origin to the Lambda env var (no redeploy needed).
- Minor: `/health-card`'s `LISTED` passport event was not observed in DynamoDB during MT3 testing (GRADED/ROUTED persist fine). Non-blocking; glance during MT4/MT5 if the listed-count matters.

## Current major task queue
MT1 ✅ → MT2 ✅ → MT3 ✅ (⭐ spine LIVE) → MT4 ✅ (every demo beat reachable) → MT7 ✅ (two-sided console, commit de98e75) → MT9 ✅ (web console — de-phoned, photo upload, buyer storefront) + landing-restore fix (6ea6749) → MT8 ✅ (dark-store node + derived cascade, commit ddce925) → **MT10 ✅ (buyer-flow fixes + resell redesign, commits ..2d1e1fa)** → **Grading hardening ✅ (Gemini 2.5 Flash primary, delta-honest prompt, same-unit gate — June 14)** → **MT5 (bulletproof, now incl. MT7+MT9+MT8+MT10 + the grading hardening's failover drill — BUILD NEXT)** → MT6 (submission). One MT per session; end-of-session protocol: commit → update STATE/tasks/lessons → handoff → new session. (MT7/MT8/MT9 are numbered after MT6 but RUN before MT5/MT6 — net-new scope; MT5/MT6 keep their numbers. The phone frame is GONE as of MT9 — MT8's cascade strip + dark-store label render in the web RouteScreen.)
